# Managed Pi Named Tokens Review Prompt

You are the authentication-lifecycle boss: part Nix graph surgeon, part crash-consistency gremlin, part Pi extension whisperer. Be ruthless; this plan handles bearer credentials and live runtime switching, so polite hand-waving is the enemy.

## Your Task

Review the [Managed Pi Named Tokens plan](../dev-plans/pi-named-tokens.md) for gaps, misunderstandings, bugs, and unnecessary work. Read the actual repositories and Pi 0.84.2 docs/runtime before judging it. Make plan fixes as commits rather than merely reporting them.

## Project Context

Allod composes public Nix repositories into private development machines. This change extends the existing managed Pi provider lifecycle without publishing private integration facts.

Key repositories:

- `allod/secrets` — credential schema, ciphertext/recipient derivation, and per-VM projections.
- `allod/nexus` — the host-side `pi-provider` transactional lifecycle command.
- `allod/archetypes` — Age delivery, reconciliation, helper, generated extension, and lifecycle witness.
- `allod/deploy` — compatible public input graph and composition canary.

Current facts to verify against the tree:

- One credential can serve several providers, but each credential currently has one `<credential>.age` ciphertext and one volatile file/link.
- `pi-provider` journals exact touched paths across profiles and secrets and currently re-prompts for retargeting rather than decrypting ciphertext.
- The helper validates one RFC 6750 line and emits it only to Pi's command-backed credential stdout.
- The reconciler atomically merges managed `auth.json`, `models.json`, links, and ownership state while preserving unrelated/operator-owned data.
- Pi 0.84.2 has supported provider re-registration and non-persistent runtime key overlays; extension commands execute during streaming, and pending-message/settled APIs exist.
- Public history may contain identifiers and synthetic tokens only, never private endpoints, provider/model IDs, targets, recipient keys, selections, or bearers.

## Structural Conformance

Verify all mandatory sections from `dev-plans.md` exist: Tracking Issue, Goal, Scope, Risk Assessment, Interface Contracts, Agent Gates, Acceptance Tests, and Rollback Plan. Check per-slice residual risk, generated lifecycle witnesses, and that only the final deploy PR closes issue #36.

## Pass History

- **Pass 1** — `claude-opus-4-8` (Claude Code runner), reasoning `medium`, reviewed `a6db27f`. 3 original-plan findings, 0 review-introduced: 1 BLOCKER (ownership-manifest version transition would strand the first post-upgrade activation — the reconciler's strict `.version==1`/per-credential-link check would treat a valid prior-version manifest as corrupt and never converge), 1 GAP (token-directory lifecycle unspecified; `realpath -e` parent check, single-segment `safe_journal_path` allowlist, and file-only rollback/recover/retire leave empty untracked directories), 1 SIMPLIFY (drop dead `rotationStrategy` metadata). Fixed in `e04ccc0`. Verified against Pi 0.84.2 docs that `registerProvider` from `session_start`, `agent_settled`, and `ctx.hasPendingMessages()`/`isIdle()` exist and support the startup and queued-work pinning claims (Focus Areas 1 and 2 hold, no findings there). Fix stability: re-verified in pass 2, with two ambiguities tightened in `9897fcd`.
- **Pass 2** — `gpt-5.6` (Pi/nullsink runner), reviewed `e04ccc0` and performed a fresh sweep. 0 original-plan findings, 2 review-introduced findings: 0 BLOCKER, 2 GAP, 0 SIMPLIFY. GAP 1: the pass-1 manifest wording said to re-derive ownership from desired links but did not preserve the strictly validated old ownership proof needed to retire exact legacy credential-level links, risking either orphaned links or weakened corrupt-state refusal. GAP 2: the pass-1 claim that no untracked directory survives conflicted with preserving operator/concurrent contents and could imply unsafe recursive cleanup. Fixed both in `9897fcd` by requiring exact old-manifest/link validation solely for legacy retirement, desired-only derivation of new ownership, and empty-only `rmdir` pruning with a populated-directory race witness. The SIMPLIFY sweep reconsidered `rotationStrategy`, legacy fallback, startup flags, provider/token command separation, state helpers, deployment scope, and quota/failover machinery; no further removable scope was found. Fix stability: not yet re-verified. Stop rule: not met; this is the first consecutive pass both with review-introduced findings outnumbering originals and with neither a BLOCKER nor an original-plan GAP.
- **Pass 3** — `claude-sonnet-5` (Pi runner), reasoning `medium`, reviewed `9897fcd`. Scoped diff verification of `9897fcd` held: reading `archetypes/modules/pi-provider-reconcile.sh` confirmed the current single-token reconciler's exact old-manifest-shape check (`.version == 1`, `credentialLinks` path/target equality) matches the plan's "old strict validation" wording, and the `rmdir`-only pruning direction has no counter-evidence in the (not-yet-implemented) named-token code paths. 1 original-plan finding, 0 review-introduced: 0 BLOCKER, 1 GAP, 0 SIMPLIFY. GAP (present since `a6db27f`, missed by passes 1-2): the plan never pinned the per-token runtime credential link naming scheme. Because both credential and token names may contain hyphens, a flat `<credential>-<token>` link name cannot be split back into its parts for per-credential token listing, forcing nested `<credential>/<token>` links — but nesting means the legacy single-file per-credential symlink and the new per-credential token directory occupy the identical leaf path (confirmed against `path=$credential_dir/$credential` in the live reconciler), so the pass-1/2 manifest-upgrade fix needed an explicit retire-before-create ordering and same-path collision refusal or an implementation could `mkdir` over the still-live legacy symlink and fail the very upgrade path those passes fixed. Fixed in `31ae5c1` by pinning nested link naming, stating the retire-before-create order and same-path collision refusal, and adding a witness item for the same-path conversion. SIMPLIFY sweep: re-examined `rotationStrategy` against the live Nexus script rather than trusting pass 1's characterization — it is read in exactly three places (`stage_registries`'s add-time consistency check, the credential-registry schema's `exact([...])`/enum check, and `print_impact`'s `rotate` case, which prints an extra remote-invalidation warning only for `"in-place"`), and all three are reachable only through the provider-scoped `add`/`rotate` commands the plan already replaces with the token model, so pass 1's drop is confirmed lossless at the code level; the plan's generic follow-up guidance ("revoke the previous remote token only after verification, when the provider supports overlap") already carries the operator-facing intent of the dropped per-credential warning, so no replacement text was added. Also checked Pi 0.84.2 `docs/models.md` ("pi intentionally does not apply built-in TTL, stale reuse, or recovery logic" for `!command` values) confirming no hidden credential-command caching undermines the `/token` command-cache-separation acceptance test; no further removable scope found among legacy fallback, startup flags, command separation, state helpers, deployment scope, or quota/failover machinery. Fix stability: not yet re-verified. Stop rule: not met — pass 3 found an original-plan GAP, so neither disjunct holds (review-introduced 0 does not outnumber original-plan 1, and pass 3 is not a "neither BLOCKER nor original-plan GAP" pass); continue to a pass 4 scoped diff verification of `31ae5c1`.

- **Pass 4** — `gpt-5.6-terra` (Pi runner), reasoning `ultra`, reviewed `31ae5c1`. The same-leaf order is enforceable in one reconciliation, but not crash-consistent as written: unlink then `mkdir` necessarily has an absent-name interval, while the current reconciler has only trap rollback and strict old-manifest validation would refuse after `SIGKILL`/reboot. 1 review-introduced BLOCKER fixed in `c7a5caa`: require a synced, bearer-free conversion journal that recovery consumes before manifest validation, exact-link-only rollback with empty-only directory removal, directory syncs, and kill/reboot witnesses. The pass-3 naming/order fix therefore remains unverified and now requires a scoped diff pass by a different model. Fresh sweep found 1 original-plan GAP fixed in `c7a5caa`: defer preference-file persistence until a pending `/token` selection actually activates, so cancel cannot leave a canceled remembered token. SIMPLIFY considered conversion-journal scope versus rename-only/atomic-dir alternatives, preference-state helpers, legacy fallback, startup flags, selector/provider split, deployment work, and quota/failover machinery; the journal is necessary for the type-changing crash window and no removable scope remains. Stop rule: not met — review-introduced findings (1) do not outnumber original-plan findings (1), and this pass has both a BLOCKER and an original-plan GAP.

- **Pass 5** — `claude-opus-4-8` (Pi runner), reasoning `medium`, reviewed `c7a5caa`. Scoped diff verification held: `c7a5caa`'s conversion journal is internally sound *for the same-leaf design* — synced with its state dir before unlink, consumed before ordinary manifest validation, restores the exact validated legacy symlink or preserves a foreign/populated occupant, empty-only `rmdir`, never recursive. But the directed SIMPLIFY sweep dissolves the design it protects. 1 SIMPLIFY (removes review-introduced machinery from `31ae5c1` and `c7a5caa`), fixed in `362d3d0`: the credential-link path is an internal reconciler contract — the generated `auth.json` `!command` is emitted against the same root in the same operation (`pi-provider-reconcile.sh` line 352), so nothing external pins the legacy `<credential-link-root>/<credential>` leaf. Installing the new nested per-token links under a *distinct* managed root that never contains a legacy leaf removes the symlink-to-directory type change entirely: create the additive new links, commit auth/models/new-version manifest atomically, then retire each legacy symlink by exact recorded-target match and prune the emptied directory with empty-only `rmdir`. Every crash point is crash-safe with ordinary atomic renames and idempotent retry (pre-commit: prior-version manifest + legacy links intact, new links re-created idempotently; post-commit: new layout usable, leftover legacy symlink retired next run by provable exact-target match). This deletes the pass-3 same-path collision/retire-before-create ordering and the entire pass-4 durable conversion journal, pre-unlink sync, and restore-exact-symlink recovery while satisfying every contract (unambiguous per-credential token listing via nesting, internal auth-command consistency, ownership-manifest strictness, forward-only migration, operator-collision preservation). Fresh sweep found no original-plan finding: startup (Focus 1) and queued-work pinning (Focus 2) re-confirmed against Pi 0.84.2 `docs/extensions.md`/`rpc.md` (`registerProvider`, `session_start`, `agent_settled`, `hasPendingMessages`) and `docs/models.md` (`!command` values get no built-in TTL/stale/recovery, so command-cache separation holds); the pass-2 empty-only `rmdir` directory-object fix and the pass-4 preference-persistence-after-activation fix remain intact and correct. SIMPLIFY additionally reconsidered `rotationStrategy`, legacy fallback, startup flags, selector/provider split, preference-state helpers, deployment scope, and quota/failover machinery; no further removable scope beyond the migration journal. Fix stability: `c7a5caa` journal verified sound in scope but superseded by the distinct-root simplification; `31ae5c1` same-leaf naming superseded. Stop rule: **not met**. Disjunct 1 (review-introduced outnumber original-plan for two consecutive passes): pass 5 qualifies (1 review-introduced SIMPLIFY vs 0 original-plan), but pass 4 was 1-vs-1, so not two consecutive. Disjunct 2 (two consecutive passes with neither a BLOCKER nor an original-plan GAP): pass 5 qualifies, but pass 4 had a BLOCKER, so not two consecutive. Continue to a scoped diff verification of `362d3d0` by a different model: the distinct-root simplification *removes* crash machinery and must be independently confirmed crash-safe.

- **Pass 6** — `gpt-5.6-luna` (Pi runner), reasoning `medium`, reviewed `362d3d0` and the public archetypes reconciler, lifecycle witness, Nexus fixtures, and Pi 0.84.2 extension/model documentation. 1 original-plan finding, 0 review-introduced findings: 0 BLOCKER, 1 GAP, 0 SIMPLIFY. GAP 1: the distinct-root migration was described as atomically committing `auth.json`, `models.json`, and the ownership manifest, although the live reconciler can only publish them with separate renames; without a required ordering, a kill could publish auth that names links not yet created and the crash witness would not prove safe intermediate states. Fixed in `293362d` by requiring additive link creation first, then separate auth/models/manifest publication, and by requiring kill injection at every link and rename boundary with retry convergence and no auth reference to an absent new link. The distinct-root design itself remains crash-safe under that ordering: it never removes the legacy leaf before additive new links exist, and post-manifest legacy retirement is exact-target, idempotent, and empty-only. Foreign occupants are preserved/refused by the existing no-clobber contract, but the generated witness must exercise both legacy and nested-new collisions. Fresh sweep found no further blocker, gap, or unnecessary scope. SIMPLIFY reconsidered conversion journals, pre-unlink sync, restore-symlink recovery, same-leaf collision handling, legacy fallback, startup flags, provider/token separation, preference helpers, deployment scope, and quota/failover machinery; no further removable work found. Fix stability: the distinct-root simplification from `362d3d0` survived the crash-consistency review after its publication ordering was made explicit; the wording fix in `293362d` is not yet independently re-verified. Stop rule: **not met** — review-introduced findings (0) do not outnumber original-plan findings (1), and this pass contains an original-plan GAP. Continue with a scoped verification of `293362d` by a different eligible model, then a fresh sweep.

- **Pass 7** — `claude-sonnet-5` (Pi runner), reasoning `medium`, reviewed `293362d`. Scoped diff verification: traced the publication ordering (additive new links, then separate auth/models/manifest renames, then legacy retirement) against the actual single-token reconciler in `archetypes/modules/pi-provider-reconcile.sh`, which retires no-longer-desired legacy links from the pre-mutation manifest snapshot *before* it overwrites that manifest. The plan's migration path inverts this for legacy per-credential links, retiring them *after* the new-version manifest commits — but the old manifest's `credentialLinks` list is the only place a legacy target was recorded, and that list is gone once the new manifest lands. 1 original-plan GAP, 0 review-introduced: a credential retired in the same operation that performs the version bump would never appear in any future desired-credential set, so retirement keyed off "currently desired credentials" could never find and clean up its legacy link, and a crash between the manifest commit and retirement had no persisted list to resume from. Fixed in `5ed45c6` by making legacy retirement a standing, credential-set-independent sweep of the legacy link root's direct entries — retiring any entry that is a symlink to its own name's computed runtime target, regardless of current desired status, run unconditionally on every activation rather than only right after a version transition — so it self-heals from the directory alone with no persisted list, across both a crash between manifest-commit and retirement and simultaneous credential removal. Added the combined migration+removal case to the archetypes acceptance-test witness. Fresh sweep re-confirmed the rest of `293362d`'s ordering (additive-link-first, no auth referencing an absent new link, empty-only `rmdir`, foreign-occupant preservation) and found no further issue in Focus Areas 1, 2, 3, 5. SIMPLIFY: reconsidered the sweep-based fix itself against a per-desired-credential loop (rejected: cannot find removed credentials), a persisted pending-retirement list in the manifest (rejected: reintroduces exactly the journal-like bookkeeping pass 5 removed, and the directory is already self-describing), conversion journals, pre-unlink sync, restore-symlink recovery, same-leaf collision handling, legacy fallback, startup flags, provider/token separation, preference helpers, deployment scope, and quota/failover machinery; no further removable scope found. Fix stability: `293362d`'s publication ordering survived; the `362d3d0` distinct-root design's crash-safety claim required this correction to hold under simultaneous credential removal. Stop rule: **not met** — review-introduced findings (0) do not outnumber original-plan findings (1), and this pass found an original-plan GAP. Continue with a scoped diff verification of `5ed45c6` by a different eligible model than `claude-sonnet-5`, focused on whether the unconditional directory-wide sweep can ever misidentify or race against a legitimate concurrent per-token link create under the new root (it operates only on the legacy root, which the new code never writes to, so this should be uncontested, but confirm against the eventual implementation rather than the plan prose alone), then a fresh sweep.

- **Pass 8** — `gpt-5.6-sol` (Pi/nullsink runner), reasoning `medium`, reviewed `5ed45c6`, the live archetypes reconciler/lifecycle witness, Nexus and secrets contracts, deploy references, and Pi 0.84.2 documentation/runtime surfaces. Scoped diff verification found 0 original-plan findings and 1 review-introduced finding: 1 BLOCKER, 0 GAP, 0 SIMPLIFY. **[BLOCKER]** Pass 7's unconditional legacy-root sweep violates the ownership contract it was meant to preserve: a syntactically valid, operator-created symlink can independently point to the same computed runtime target, and path/target coincidence does not prove that the old manifest ever recorded it. The proposed sweep would delete that unrecorded object. This differs materially from the live reconciler, whose v1 manifest strictly records exact credential-link paths and targets and whose cleanup is bounded by those records. Fixed in `b6d95ec`: strictly validated v1 `credentialLinks` are copied into a bounded transitional `legacyCredentialLinks` map in the first v2 manifest, including credentials removed during the version bump; retirement processes only that durable map, accepts absent entries after a crash, preserves/refuses changed occupants, and atomically clears the map after all recorded links are absent. New-version validation never discovers or augments ownership by scanning the legacy root. Acceptance coverage now includes simultaneous removal, malformed transitional state, kills at both manifest publications and each recorded unlink, and an unrecorded exact-target operator symlink that must survive. The pass-6 additive-link/auth/models/manifest ordering remains intact, and the bounded map closes pass 7's crash/resume and simultaneous-removal gap without the pass-4 same-leaf conversion journal, pre-unlink sync, or restore-symlink recovery. Fresh sweep found no further finding in startup, queued-work pinning, token semantics, directory pruning, preference concurrency, generated witnesses, rollback, or privacy boundaries; Pi 0.84.2's cited `registerProvider`, `session_start`, `agent_settled`, pending-message, and command-backed-auth surfaces remain available. SIMPLIFY reconsidered the unconditional scan, desired-set lookup, carrying old ownership in the ordinary manifest, a separate journal, pre-unlink sync, restore-exact-symlink recovery, same-leaf conversion, legacy fallback, startup flags, selector/provider separation, preference helpers, deployment scope, and quota/failover machinery. The transitional manifest field is the minimum bounded ownership state needed across the post-publication cleanup boundary; no further removable scope was found. Fix stability: `5ed45c6` is rejected and superseded for ownership discovery; `293362d`'s publication ordering and `362d3d0`'s distinct-root simplification survive. Stop rule: **not met**. Pass 8 qualifies for the first disjunct (1 review-introduced vs 0 original-plan), but pass 7 did not, and the BLOCKER prevents the second disjunct. Continue with a different-model scoped diff verification of `b6d95ec`, then a fresh sweep.

- **Pass 9** — `claude-opus-4-8` (Pi runner), reasoning `medium`, reviewed `b6d95ec` (scoped diff verification of the pass-8 BLOCKER fix by a different model than `gpt-5.6-sol`), then a fresh sweep. Scoped diff verification **held**: read the live `archetypes/modules/pi-provider-reconcile.sh` and confirmed the v1 ownership manifest strictly records every managed legacy link with exact `credentialLinks[].path`/`.target` (`path == root + "/" + key`, `target == runtime + "/pi-provider-credential-" + key`), so strict v1 validation genuinely supplies the *complete and only* legacy ownership set that `b6d95ec` copies into the bounded transitional `legacyCredentialLinks` map — including credentials removed in the same activation, since v1 `credentialLinks` reflects the pre-mutation desired set. Traced every crash boundary: pre-first-v2-manifest crashes leave v1 + all legacy links intact (the strict pre-mutation presence check applies only on the v1 path, keyed by manifest version); post-first-v2-manifest crashes resume bounded retirement that tolerates absent recorded paths, removes only exact recorded symlinks, and preserves/refuses foreign occupants; the second atomic manifest replacement clears the map only after all recorded paths are absent. Confirmed retirement reads *only* the durable map and never a directory scan, so an unrecorded operator symlink with the exact conventional name/target survives (the pass-8 BLOCKER is closed), empty-only `rmdir` remains the sole pruning, and no conversion journal, pre-unlink sync, restore-exact-symlink recovery, same-leaf conversion, or desired-set-keyed retirement returns. Re-confirmed Pi 0.84.2 `docs/extensions.md` still exposes `registerProvider`/`unregisterProvider` (immediate, no `/reload`), `session_start` (before the provider-registration flush and before the first request), and `agent_settled`/`ctx.isIdle()`/`ctx.hasPendingMessages()` (lines 560/1019: `agent_settled` fires only when Pi will not continue automatically and `isIdle()` is false during queued continuation), sustaining Focus 1 and Focus 2. 1 original-plan finding, 0 review-introduced: 0 BLOCKER, 1 GAP, 0 SIMPLIFY. GAP (present since the pass-3 nested runtime design in `31ae5c1`, missed by passes 3-8): the plan makes the Nexus secrets token directory a first-class journaled object with empty-only `rmdir` pruning (line 60) and prunes the *legacy* runtime directory on migration (line 93), but never states any pruning rule for the *new* runtime nested `<token-link-root>/<credential>/` subdirectories the nested design forces the reconciler to `mkdir`. Line 81's "removes only paths recorded in its ownership manifest" covers links, not directories, so a credential's last-token removal or full retirement strands an empty nested subdirectory (and the token-link root at final empty state) — the exact defect class pass 1 policed for the Nexus directory, never applied to the runtime nested directory, silently undermining the plan's own "final empty state" and "partial token/provider removal" contracts. Fixed in `f917884` by requiring the reconciler to own the token-link root and each `<credential>/` subdirectory it creates, prune emptied ones (and the root at final empty state) with empty-only `rmdir` after the managed links are gone while preserving/reporting operator/concurrent contents and never recursing, and self-heal a leftover empty directory from an interrupted run on the next reconciliation; added partial-removal, full-retirement, and populated-nested-directory coverage to the archetypes witness. SIMPLIFY sweep reconsidered the transitional `legacyCredentialLinks` map (kept: minimum bounded ownership state across the post-publication cleanup boundary), a directory-wide scan (rejected by pass 8), a durable journal / pre-unlink sync / restore-symlink recovery (superseded by the distinct-root design), same-leaf conversion, desired-set-keyed retirement, `rotationStrategy`, legacy fallback, startup flags, selector/provider split, preference-state helpers, deployment scope, and quota/failover machinery; no further removable scope found. Fix stability: `b6d95ec` bounded-map ownership verified sound and closes the pass-8 BLOCKER; `293362d` publication ordering and `362d3d0` distinct-root design survive. Stop rule: **not met** — review-introduced findings (0) do not outnumber original-plan findings (1), and this pass found an original-plan GAP. Continue with a scoped diff verification of `f917884` by a different eligible model than `claude-opus-4-8`, focused on whether empty-only `rmdir` pruning of the nested runtime directories can ever race a concurrent per-token link create under the same `<credential>/` subdirectory or misreport a populated directory, then a fresh sweep.

- **Pass 10** — `claude-opus-4-8` (Pi/nullsink-claude runner), reasoning `medium`, reviewed `f917884`/`5622359` and executed the directed decisive SIMPLIFY of the entire runtime-link design against the live `archetypes/modules/pi-provider-reconcile.sh`, `pi-provider-lifecycle.nix`, `pi-provider-credential-helper.sh`, the Nexus `pi-provider` script, and Pi 0.84.2 `docs/custom-provider.md`/`extensions.md`. 1 finding, classified review-introduced (it removes machinery accreted across passes 3-9): 0 BLOCKER, 0 GAP, 1 SIMPLIFY, fixed in `72dbcff`. **[SIMPLIFY]** The managed runtime credential-link layer is pure indirection and can be deleted. Confirmed the credential-link path is an internal reconciler contract — the generated `auth.json` `!command` is emitted against `$credential_dir` in the same operation (`desired_auth`), so nothing external pins it — and that agenix already publishes each credential at a fixed, non-secret volatile path `${credentialRuntimeRoot}/pi-provider-credential-<cred>` (`pi-provider-lifecycle.nix` `ageSecrets`) that the reconciler already reads and validates directly. `docs/custom-provider.md` confirms `!command` executes for the whole config value against any path argument, and `docs/models.md` gives it no TTL/caching, so the auth and `/token` selector commands can address the fixed volatile per-token path `<runtime-root>/pi-provider-token/<credential>/<token>` directly (paths/names non-secret, never parsed back, so the flat-name hyphen ambiguity that forced nested links in pass 3 never arises). This removes the distinct token-link root, nested runtime directories and their empty-only `rmdir` pruning (the entire pass-9 fix), the manifest-version migration, the bounded transitional `legacyCredentialLinks` map (pass 8), and all associated conversion-journal/pre-unlink-sync/restore-exact-symlink crash machinery (passes 4-6). The manifest stays at v1: `credentialLinks:{}` passes v1 validation vacuously (verified with `jq`); the strictly validated prior manifest is the complete and only ownership proof, and the existing transaction retires every recorded legacy per-credential link (regardless of desired status) and records `credentialLinks` empty. The prior manifest persists until the new one lands, so a crash re-drives the same bounded idempotent retirement with no new journal, and an unrecorded operator legacy-root symlink survives even when name/target coincide. Bearer handling unchanged (stdout pipe only, never argv/generated files). Fresh sweep: startup (Focus 1) and queued-work pinning (Focus 2) re-confirmed against `registerProvider`/`session_start`/`agent_settled`/`hasPendingMessages`; the Nexus secrets-checkout token-directory journaling (checkout side, pass 1/2) stays and is unaffected; the pass-4 preference-persistence-after-activation fix stays intact. SIMPLIFY additionally reconsidered the Nexus token-directory journal (kept: real git-checkout transaction object), `rotationStrategy`, legacy fallback, startup flags, selector/provider split, preference-state helpers, deployment scope, and quota/failover machinery; no further removable scope. Fix stability: supersedes the `362d3d0` distinct-root design, the `b6d95ec` transitional map, and the `f917884`/`293362d` runtime-link ordering and nested-directory pruning — all removed. Note: pass 9 was also `claude-opus-4-8`; this pass removes crash machinery and must be independently confirmed crash-safe. Stop rule: **not met** — pass 10 qualifies for disjunct 1 (1 review-introduced vs 0 original-plan) and disjunct 2 (no BLOCKER, no original-plan GAP), but pass 9 qualifies for neither, so no two-consecutive run holds. Continue with a scoped diff verification of `72dbcff` by a different, preferably cross-vendor model (e.g. `gpt-5.6-sol`), focused on whether direct volatile-path auth is genuinely crash-safe with only the existing v1 transaction and whether removing the runtime link layer leaves any ownership/cleanup gap, then a fresh sweep.

- **Pass 11** — `gpt-5.6-sol` (Pi/nullsink runner), reasoning `medium`, independently reviewed `72dbcff` against the live `archetypes/modules/pi-provider-reconcile.sh`, lifecycle module/witness, Nexus transaction, secrets schema/projections, deploy graph, and Pi 0.84.2 documentation/runtime surfaces, then performed a fresh sweep. Direct volatile-path auth itself is valid: agenix owns the fixed per-token path before reconciliation, generated auth and `/token` commands need no managed runtime link, v1 accepts empty `credentialLinks`, and the Nexus checkout-side nested token-directory transaction remains independently necessary. 0 original-plan findings and 1 review-introduced finding: 1 BLOCKER, 0 GAP, 0 SIMPLIFY. **[BLOCKER]** Pass 10 put the empty-link manifest rename before legacy-link retirement while simultaneously claiming the prior manifest would remain available for retry. The live v1 manifest is the only ownership proof; a kill after that rename but before any/all unlinks permanently discards the recorded set, so subsequent activation cannot safely distinguish and retire the stranded links. Fixed in `fc6cfe5` without restoring any removed runtime-link machinery: publish direct-path auth and models first, retire only links recorded by the still-current strict v1 manifest, accept an absent recorded path as an already-completed unlink on kill/retry, refuse foreign occupants, and publish `credentialLinks:{}` only after every recorded path is absent. Ordinary trapped failures restore snapshotted files and exact pre-run links. The witness now injects kills after auth/models renames, each unlink, and the final manifest rename, plus ordinary rollback and foreign-occupant cases. Fresh sweep found no further finding in Pi startup/provider re-registration, queued-work pinning, selector preference concurrency, secrets privacy/projection, Nexus two-segment allowlists and empty-only directory pruning, deploy composition, final retirement, or rollback. SIMPLIFY reconsidered a transitional map, conversion journal, distinct runtime-link root, manifest-version migration, pre-unlink sync, special SIGKILL restore, runtime-directory pruning, legacy fallback, startup flags, selector/provider separation, preference helpers, deploy scope, and quota/failover machinery; none is needed beyond the corrected ordering. Fix stability: the core `72dbcff` no-runtime-link simplification survives, but its crash-order claim is rejected and corrected. Stop rule: **met** — passes 10 and 11 satisfy disjunct 1 (each has 1 review-introduced finding vs 0 original-plan findings), so this review sequence terminates under the two-consecutive-pass rule. Because pass 11 made a BLOCKER-level correction, any resumed review must begin with a scoped diff review of `fc6cfe5` by a model other than `gpt-5.6-sol` before attempting another fresh sweep.

## Focus Areas

The six standing lenses from `dev-plans.md` apply, plus:

1. **Startup before first request.** Verify Pi actually loads/authenticates models and extensions in an order where the generic selector plus `session_start` can resolve remembered/default/interactive choice before any request. Flag any UI/non-interactive path the proposed APIs cannot implement.

2. **Queued-work pinning.** Inspect event ordering and provider re-registration. Can `agent_settled` plus pending-message state truly keep the active request and every already queued message on the old token without delaying forever or switching one provider early?

3. **Nexus secrets-checkout token-directory transaction (verify pass-2 fix).** The runtime nested-link directories and their pruning were removed in pass 10, so only the *checkout-side* directory object remains in scope. Pass 2 clarified the pass-1 fix: `pi-provider` journals the Nexus secrets token directory (`secrets/pi-credentials/<credential>/`) pre-run absence, and prunes only through empty-directory `rmdir` semantics after managed ciphertext files are gone, preserving and reporting operator/concurrent contents. Verify migrate, rollback/recover, last-token remove, and retire honor this, and that the recover allowlist and recoverable-plan checks recognize the two-segment `<credential>/<token>.age` layout.

4. **Direct volatile-path auth crash-safety (verify pass-11 BLOCKER fix).** Pass 10 deleted the entire managed runtime credential-link layer; pass 11 retained that simplification but corrected its unsafe publication order. Generated auth and `/token` selector commands address the fixed, non-secret volatile per-token path `<runtime-root>/pi-provider-token/<credential>/<token>` directly, since agenix already publishes it and nothing external pins the old link path. Verify this is genuinely crash-safe with only the existing v1 transaction: `auth.json` and `models.json` publish first, every exact link recorded by the still-current strict prior manifest is retired (absent means an interrupted retirement already completed that unlink; any foreign occupant refuses), and only then does the final atomic manifest rename record `credentialLinks:{}`. Confirm ordinary rollback restores exact pre-run links/files, a kill after any unlink leaves ownership proof available for retry, an unrecorded operator legacy-root symlink survives when name/target coincide, and no conversion journal, pre-unlink sync, special SIGKILL restore, transitional map, distinct link root, manifest-version migration, or runtime directory pruning returns. Exercise kills after each auth/models rename, each legacy unlink, and the final manifest rename (`archetypes/modules/pi-provider-reconcile.sh`, `archetypes/checks/pi-provider-lifecycle.nix`, `pi-provider-lifecycle.nix`).

5. **Generated ownership and preference concurrency.** Check the token catalogue and selector (no runtime token links exist), extension installation/removal, preference locking/atomic writes, pending selection persistence only after successful activation, cancel behavior, absent secrets, corrupt state, rebuild/reboot, final empty state, and operator collisions. Demand executable generated-artifact witnesses rather than source-only assertions.

6. **SIMPLIFY.** Look for commands, metadata, compatibility paths, state helpers, or deployment work that can be deleted while preserving the settled issue contract. Do not add generic quota/failover machinery.

## Review Guidelines

- Forward momentum is king; flag implementation blockers and real rework, not style.
- This is pre-alpha and migration is forward-only; do not demand legacy fallback.
- Security boundaries are strict, ceremony is not.
- One human operates this system; avoid team/release abstractions.
- Inspect generated Nix/Home Manager artifacts and negative lifecycle paths.
- Do not re-open settled provider-versus-token semantics or add startup override flags without concrete code evidence.

## Severity Rubric

Use `[BLOCKER]` for unsafe/destructive behavior, nonfunctional startup/activation/migration/rollback, a privacy breach, or missing human input. Use `[GAP]` for material ambiguity or test/rollback holes. Use `[SIMPLIFY]` for removable scope or abstraction. Use `[QUESTION]` only when the answer cannot be inferred from code or documented decisions.

## Deliverable

Every pass ends with plan-file commits for real findings (or explicit no findings), then a review-prompt commit updating Focus Areas and pass metadata, and a push. Number findings, tag them, put blockers first and questions last. Resolve inferable questions directly in the plan.

The review-prompt commit message must count new findings by tag, classify each as original-plan or review-introduced, include fixing hashes, state what the SIMPLIFY sweep considered, and end with `Model: <exact model>`.

Stop when review-introduced findings outnumber original-plan findings for two consecutive passes, or two consecutive passes produce neither a BLOCKER nor an original-plan GAP. A blocker-level fix requires a scoped diff pass by a different model next.

## Before Final Response

- Plan fixes are committed or no-findings is explicit.
- This prompt is updated and committed with findings metadata.
- The repository is pushed and clean.
