# microVM Runtime Credential Delivery

## Tracking Issue

[allod/archetypes#29: Deliver microvm guest credentials at boot from host memory, not agenix](https://forge.anarch.diy/allod/archetypes/issues/29)

Third slice of milestone 4 in [microvm-framework-adoption.md](microvm-framework-adoption.md), tracking [allod/strategy#20](https://forge.anarch.diy/allod/strategy/issues/20). It implements that plan's **contract 7**, the guest half of **8a**, and contracts **11, 12, 14** in full; it implements **9** and **10** in part. It inherits 2, 6, 13 and 21 as constraints. One PR in `allod/archetypes` carrying `Closes allod/archetypes#29` and `Refs allod/strategy#20`; it does not close allod/strategy#20.

**Where the claim is narrowed, and why it is narrowed rather than met.** Contract 9 requires *any registered GitHub credential target* to be configured to runtime files; this slice refuses a non-empty GitHub target set at evaluation instead, so it covers 9 for the Forgejo credentials and the Forge SSH identity and not for GitHub. Contract 10 requires a bad credential to fail its dependent consumer; the two-unit split satisfies that for the host key and deliberately does not for the Forge credentials. Both are recorded as contradictions 4 and 5 below with their follow-ups named. A tracking line that claimed 9 and 10 outright while the body refuses half of one and declines half of the other would be the plan disagreeing with itself.

## Goal

A machine that selects the microvm runtime receives its private key material from the host at every boot, holds no age identity, writes no credential-derived plaintext outside one runtime root, and fails visibly instead of minting a replacement SSH identity when a credential does not arrive.

## Scope

In scope, all in `allod/archetypes`:

- `allod.microvm.guestCredentialRoot`: a typed, validated, microvm-only option (default `/run/allod/credentials`) that every consumer path derives from.
- A closed credential-name set per machine, derived from the credentials that machine actually declares.
- **Two** materializer systemd services, not one: `allod-microvm-host-key.service` owns the SSH host key alone and is what sshd depends on; `allod-microvm-forge-credentials.service` owns every other credential, and no consumer requires it — only `sysinit-reactivation.target` does, by design. Each loads the delivered system credentials it owns, validates them, and writes the consumer files atomically with declared owner and mode. Contract 6 states why the split is mandatory rather than tidy.
- Microvm-only rewiring of six consumers: sshd's `HostKey`, the Nix netrc path, root's Git credential lookup, the user's Git credential lookup, the Forge API token path, and the Forge SSH `IdentityFile` in the generated Home Manager output.
- **A small read-only Git credential helper**, generated into the store and named by both Git config files. It answers `get` by delegating to `git-credential-store`, and consumes `store` and `erase` without touching the file. Contract 4 gives the measurement: the stock `store` helper writes as well as reads and resets the file's permissions to exclude other users, which on a shared `root:users` file is an active hazard rather than a stylistic preference.
- A runtime parameter on `modules/home-shared.nix`, so the *user's* generated `git/config` carries the same credential-helper reset the system file does, and a `targets` parameter on `modules/github-credentials.nix`, so the contract 8 refusal has a fixture that can reach it. Those two are the only archetypes files other than `flake.nix` this slice edits, and both leave their libvirt output unchanged.
- Microvm-only removal of agenix wiring: no `age.secrets` definition, no `age.identityPaths` definition, no `modules/netrc.nix`.
- **A microvm-only no-swap assertion**, `swapDevices == []` and `zramSwap.enable == false`. Contract 11 gives the measurement: the materializers write into ordinary `/run`, which pinned systemd does *not* mount `noswap`, so this is the only thing standing between a materialized private key and a swap file.
- Assertions over the merged configuration for every rule above. Path and name validation is hoisted into pure functions and exhaustively table-tested; each *assertion family* keeps one full-NixOS sabotage fixture pinned to the diagnostic it names, rather than one fixture per invalid input. See the memory budget.
- Generated-artifact checks: the two rendered materializer units, their disjoint `LoadCredential=` sets, their ordering edges, the `.wants` and `.requires` symlinks that pull each unit into boot and into reactivation, the exhaustive set of inbound edges each materializer carries, the `Requires=` edge sshd carries on the host-key unit *and only on it*, the rendered `sshd_config`, the rendered `nix.conf`, both rendered Git config files, the credential helper's own behaviour run against a fixture file, the rendered `~/.ssh/config`, and a closure scan of a microvm guest.
- Rework of the existing `credential-profiles`, `netrc-activation` and `dev-forge-opt-out` checks so they are explicitly libvirt-scoped rather than accidentally so. That is three concrete edits, not a comment: `dev-forge-opt-out`'s `withTokens` passes `runtime = "libvirt"` explicitly instead of inheriting whatever inventory says; `credential-profiles` filters `profileOwnedForgejoTokenTargets`, `profileOwnedGithubTargets` and `localRefreshErrorsForRef` on `machines.<system>.runtime == "libvirt"`; and `netrc-activation` states which runtime its expectations describe and asserts the complementary shape for the other. Tests 24 to 26 carry the detail.

Out of scope, other slices of milestone 4: contract 8b's `extendModules` host integration that supplies the actual `microvm.credentialFiles` *values*; contract 15's guest networking and `microvm.socket`; contract 1's `vmFacts.<name>.runtime`; contract 18's runtime-dispatched rotation in `allod/nexus`; and the nested-boot lifecycle tests.

Not in scope anywhere in this repo: the host-side preparation, mount, principal and namespace. `allod/nexus` `nix/microvm/host.nix` and `nix/microvm/launcher.nix` own all of it and are already merged.

**Explicitly not changed: `allod/profiles`.** Its `hosts/dev/allod-dev/home.nix` sets `programs.ssh.matchBlocks.<forgeHost>.identityFile = "~/.ssh/${identity.sshKeyName}"`, a persistent-mount path, for both runtimes. The parent plan's scope gives profiles its own line, and a second repo in this PR would put the diff across a boundary that the drvPath no-op cannot cover. This slice therefore *overrides* that value for microvm from the archetypes side with `lib.mkForce`, and proves the override in the rendered `~/.ssh/config`. The profiles edit that makes the override unnecessary is a later, separable change; until it lands, a microvm guest's Forge identity path is correct because archetypes forces it, not because profiles agrees.

## Risk Assessment

Residual risk: **R3 High**, and it does not come down with the validation in this plan.

This is a security boundary change: it moves where a VM's private key material comes from and where its plaintext lands. The parent plan scores the archetypes guest integration R3 for exactly this row. Three properties keep the *blast radius* small and none of them lower the score:

- Every libvirt machine is untouched, proved by four unchanged derivation paths.
- No public machine selects microvm, so the new path's coverage is fixtures only.
- Rollback is a straight revert of one PR in one repo, and nothing here creates, moves or deletes key material.

What keeps it R3 is what the validation cannot reach. Nothing in this slice boots a guest. Every claim about credential *receipt* — that PID 1 imports the fw_cfg credentials, that `LoadCredential=` finds them, that both materializers run early enough and in the right order relative to each other, that sshd starts with a `HostKey` under `/run` — is an inference from pinned upstream source, not a measurement. The parent plan's acceptance test 9 and the nested-boot slice are what settle those, and this PR must not be read as evidence for them.

**The gate:** no machine is enabled on the microvm runtime on the strength of this change. A guest whose materializer is subtly mis-ordered comes up with sshd failed and no way in; a guest whose materializer is mis-ordered the *other* way comes up with a working sshd and a stale or absent credential, which is worse because it looks fine. Neither is detectable from this repo's checks.

Splitting the materializer in two (contract 6) narrows the first of those: a broken *Forge* credential can no longer take sshd down with it, so the guest stays reachable for repair. It does not narrow the second at all, and it adds a second unit whose ordering can itself be wrong. The score does not move.

**The worst credible failure after this plan's validation passes** is a microvm guest that evaluates and builds cleanly but, on a real boot, either strands with sshd failed, or starts sshd against a credential the host-key materializer wrote with the wrong owner or mode. Both are contained to a machine nothing depends on, per the parent plan's rule that the first microvm machine is purpose-made. Neither can affect a libvirt machine, a host, or any encrypted source.

Two failure shapes that belonged on that list are off it because this plan now closes them, and both are closed by something a check can exercise rather than by argument. A Git helper that rewrites the shared credential file — stranding the unprivileged user after a successful *root* fetch, with nothing failing at build time — is designed out by contract 4's read-only helper and measured by test 19's behaviour derivation. Materialized plaintext paging to disk is asserted against by contract 11 and sabotaged at evaluation. Neither moves the score, because neither was what kept it at R3: what keeps it at R3 is still that nothing here boots a guest.

Human scrutiny, in order:

1. The two rendered materializer units: each `LoadCredential=` source path, that the two sets are disjoint and together are exactly the declared set, each unit's `Before=`/`After=` lines *and* the `.wants`/`.requires` symlinks that pull it into `sysinit.target` and `sysinit-reactivation.target` — the Forge unit's boot-time `wantedBy` is its only start path, and losing it is silent — the `Requires=` edge sshd carries on the host-key unit and the absence of any edge to the Forge unit, and the exact `install`/`mv -T` invocations in each script.
2. The rendered `sshd_config` for a microvm fixture: exactly one `HostKey` line, pointing under the credential root, and `sshd-keygen.service` rendered as a mask.
3. The rendered `nix.conf`: the *last* `netrc-file =` line is the runtime path, and every unrelated inherited `extraOptions` line is still there.
4. Both rendered Git config files — `/etc/gitconfig` and the dev user's `git/config` — for the helper reset, the read-only helper after it, and no helper in either file that can *write* the shared credential file. A helper that writes is the one way this design can strand the unprivileged user without anything failing at build time; contract 4 has the mechanism.
5. The no-swap assertion, because it is the whole of the guest's protection for materialized plaintext at rest and nothing else in the plan notices if it is deleted.
6. The four libvirt derivation paths.

## Contract contradictions found

Five, all recorded rather than worked around.

1. **Option namespace.** Contract 8a names the guest option `allod.microvm.guestCredentialRoot`. The volumes slice that landed after contract 8a was written chose `allod.archetypes.microvm.volumeImageRoot`, with a comment at the option's declaration in `microvmVolumesModule` arguing the namespace should name the owning repo. Both are archetypes-owned options on the same configuration, so the repo now has two conventions. This plan follows contract 8a literally, because the contract names the exact string and nothing in the code depends on the other spelling. The reconciliation — most likely renaming the volumes option — is a separate cosmetic change, not this one.

2. **Contract 9 lists `/etc` as forbidden, and `/etc` on a microvm guest is tmpfs.** The microvm root is the upstream tmpfs root (contract 4), so `/etc/nix/netrc` on a microvm guest is no more durable than `/run/allod/credentials/netrc`. The contract is still followed literally: the value of one runtime root is that it is one *scannable* root, so a check can enumerate credential-derived plaintext by looking in exactly one place. A second location that happens to be non-durable defeats that even though it leaks nothing. No change requested.

3. **Contract 7 requires rejecting a "duplicate normalized name", and no normalization exists to duplicate through.** Measured on the pinned sources, not argued: nothing between the guest declaration and the guest's `/run/credentials/@system` entry ever folds two distinct names together. `microvm.credentialFiles` is an `attrsOf`, so its keys are Nix attribute names — already unique by construction, and a Nix attrset literal with a repeated key is an evaluation error, not a silent merge. microvm.nix's runner interpolates each key verbatim into `name=opt/io.systemd.credentials/<name>,file=<path>` with no case folding, trimming, or substitution. systemd's `credential_name_valid` (`src/shared/creds-util.c:49-53`) *validates* the exact string and rejects what fails; it does not rewrite one name into another, and `import-creds.c` writes the file under the name it read. So "two names that collide after normalization" has no reachable input on the guest side: to construct one, an implementer would have to invent a normalization step the framework does not perform, purely so a fixture could violate it.

   What this slice does instead: it asserts the name rules that *are* reachable — non-empty, at most 28 characters, `[A-Za-z0-9_-]+`, no `.` — over the union of the declared set and any supplied `credentialFiles` keys, and it carries no collision fixture. Acceptance test 7 previously demanded one; that case is removed.

   What it implies for the parent contract: contract 7's "duplicate normalized name" clause is either unreachable and should be dropped, or it names a normalization operation the framework has yet to define — most plausibly on the *host* side, where contract 8b derives a value per machine and per name and a real folding step could be introduced. **This plan does not edit the parent plan**, which converged at its own review pass 10; correcting or deleting that clause goes through the parent's normal change process, and this section is the empirical input to it.

4. **Contract 10 requires a bad credential to fail its consumer, and the two-unit split deliberately does not for Forge credentials.** Parent contract 10 ends: *"A required missing, empty, malformed, or wrongly owned credential makes the materializer and dependent consumer fail visibly."* Under the split (contract 6) the host-key half satisfies that in full — sshd `Requires=` the host-key unit, so a bad host key leaves both the materializer and its consumer failed. The Forge half does not: a broken Forge credential fails `allod-microvm-forge-credentials.service` and, by explicit design, **no consumer unit fails with it**. No consumer carries a `Requires=` on that unit — `sysinit-reactivation.target` does, and a rebuild target is not a consumer — so git, Nix-over-HTTPS and the `forge` CLI fail at the point of use instead, with the failed unit in `systemctl --failed` as the only unit-level signal.

   This is the trade contract 6 argues and the Risk Assessment owns, and it is the right one: behind a single unit, any optional Forge credential can stop the host key from ever being published, so a machine loses sshd and becomes unreachable over a credential nothing needs to boot. It is nonetheless a strict-reading deviation from a converged parent contract, so it is recorded here rather than left as an unstated exception — and recording it is what stops a later review pass re-litigating the split against the parent's letter. One reading avoids the deviation entirely: contract 10 says *required*, and no Forge credential is required for the guest to come up. The reconciliation — narrow that clause to the credential whose absence must stop a service, or state that "dependent consumer" means the units that `Requires=` the materializer and no others — goes through the parent's own change process, not this plan.

5. **Contract 9 requires every registered GitHub credential target to be configured to runtime files, and this slice refuses them instead.** The parent's words are "any registered GitHub credential target", which is a requirement to *serve* such a target from the credential root. Contract 8 below does the opposite: `(mkGithubCredentialModule name)` stays composed, and a microvm machine whose GitHub target set is non-empty **fails evaluation** with a named diagnostic. That is a refusal, not an implementation, and the Tracking Issue section says so rather than claiming contract 9 whole.

   Why refuse rather than implement. A GitHub target names a destination path and a credential the *host* would have to supply, and the public `secrets` template pins `githubCredentialTargets` empty for every machine, so there is no machine and no registry entry to derive a credential name from. Building the path anyway would mean inventing the host-side name, the delivery shape and the consumer file with no reachable configuration to validate any of them against — speculative generality whose only test would be a fixture asserting the shape the same commit invented. Refusing costs one assertion, is loud (`architecture.md` principle 11), and cannot silently deliver a GitHub credential to a persistent path in the meantime, which is the failure contract 9 exists to prevent.

   **The follow-up, named.** The registered-GitHub-target path lands with contract 8b's host integration, in the same slice that teaches the host to derive a value per machine and per credential name — that is the first point at which a GitHub target has a host-side supplier to be wired to. Until it lands, a private inventory that registers a GitHub target for a microvm machine gets an evaluation failure naming this slice's diagnostic, which is the intended and only correct outcome. As with contradictions 3 and 4, **this plan does not edit the parent plan**; whether contract 9's GitHub clause moves to the 8b slice or stays where it is goes through the parent's own change process.

## Interface Contracts

Inherited and not restated: contracts 2, 6, 13, 21.

**How to read the citations below.** Anything in a pinned store path — nixpkgs, systemd, QEMU, microvm.nix, agenix, the git documentation — is cited by file and line, and those are stable because the revision is. Anything in a working tree is cited by **identifier and quoted construct** instead, and the rule is absolute rather than a habit for one file. `allod/archetypes` `flake.nix` is the worst case — it grew from ~1879 to over 2000 lines between this plan being written and its first review, and three line citations in the original draft had already drifted onto unrelated code by then — but it is not the only one: `allod/nexus`' microVM host module and launcher move under `allod/nexus` issues #21 and #22 in this same arc, and every `allod/nexus`, `allod/vm`, `allod/profiles` and `allod/archetypes` `modules/` citation below was converted to identifier form for that reason. Locate working-tree constructs by searching for the name, and re-derive any line number before quoting one in the implementation PR.

### 1. The credential root is a validated, microvm-only option, gated outside the module system

`allod.microvm.guestCredentialRoot`, declared only by a module the builders append with `lib.optional (runtime == "microvm")`, exactly as `microvmVolumesModule` is appended in `mkDevVm` and `mkPrivacyVm`. `lib.mkIf` does not help: it defers the value, not the definition, and every option this slice writes to under microvm either does not exist under `qemuGuest` (`microvm.*`) or would silently render wrong content for a libvirt machine (`systemd.services`, `services.openssh.extraConfig`, `environment.etc`).

Type is `lib.types.either lib.types.str lib.types.path`, for the reason the comment above `options.allod.archetypes.microvm.volumeImageRoot` already gives ("The type accepts a Nix path it will then reject on purpose"): a bare `str` makes a Nix path fail with nixpkgs' generic type error while the option tree is built, pinnable to nothing, whereas accepting it lets a named assertion be the reporter. This deliberately diverges from the `types.strMatching` on `allod/nexus`' own `options.nexus.microvm.hostPlaintextRoot` (`nix/microvm/host.nix`), because acceptance test rule "every assertion has a paired sabotage fixture pinned to its diagnostic" cannot be satisfied by a type-check failure.

**The existing `pathErrors` cannot be reused whole, and reusing it whole is an evaluation failure on the default value.** Its sixth rule — the `lib.optional` whose message is `"must not be under ${hostRuntimeRoot}: that root is the host's tmpfs runtime state"` — rejects any path equal to or under `/run/allod`, because a volume image on the host's tmpfs runtime root cannot hold persistent state. This option's default *is* `/run/allod/credentials`, and contract 8b's supplied values are host paths under `/run/allod/microvm`. Reused unchanged, that rule rejects every correct value this slice produces.

So split it. Hoist only the five generic rules into a shared validator: the value is a plain string and not a Nix path, is absolute, has no empty segment (no trailing or doubled slash), has no `.` or `..` segment, and is not under the store. Those five are what the volume image root, the credential root and every supplied `microvm.credentialFiles` value genuinely share.

The `/run/allod` prohibition stays inside `microvmVolumesModule`, where it is one `lib.optional` on top of the shared list rather than a shared rule with an exception.

The credential root then adds the *opposite* rule, and only it has it: the value must be strictly below `/run/allod` — neither `/run/allod` itself nor anything outside it. Contract 8a requires that, and `allod/nexus` states the same shape for its host-side root: `options.nexus.microvm.hostPlaintextRoot` is declared `type = lib.types.strMatching "^/run/allod/[^/]+(/[^/]+)*$"` (`nix/microvm/host.nix`).

Supplied `microvm.credentialFiles` values get the generic rules plus one of their own (contract 3), and never the volume rule.

Hoisting the generic rules out of `microvmVolumesModule` to a shared `let` binding is in scope and is the point: two copies of a five-rule path validator in one file is the drift this contract exists to prevent. Copying the sixth rule along with them is the drift it would cause.

Hoist to the **top-level `let` of `flake.nix`**, the same scope `checks` is evaluated in, so the checks can call the validator as an ordinary function on an ordinary string. That is what makes the memory budget below work: exhaustively table-testing a pure function over thirty bad inputs costs nothing, while proving the same thirty rules through thirty forced NixOS fixtures is the measured OOM. The name validator of contract 3 is hoisted the same way and for the same reason.

### 2. The credential-name set is closed and derived, never restated

The builder derives it from what the machine has, not from a literal list:

| Name | Length | Declared when | What the host supplies |
|---|---|---|---|
| `ssh-host-key` | 12 | always | the machine's ed25519 SSH host private key |
| `forge-token` | 11 | dev, `tokenFile != null` | the Forgejo REST API token |
| `forge-https-token` | 17 | dev, `httpsTokenFile != null` | one `https://user:pass@host` credential-store line |
| `forge-ssh-key` | 13 | dev, `forgeKey != null` | the Forge SSH private key |

**Two gates, not one, and they are independent.** The two API/HTTPS names follow the two token files; the SSH name follows the machine's forge key. Nothing ties them together, and the plan must not pretend otherwise:

- `forge-token` and `forge-https-token` are gated on precisely the values that already gate `modules/agent-forgejo-token.nix` (`lib.optional (tokenFile != null)`) and `age.secrets.forgejo-https-token` (`lib.optionalAttrs (httpsTokenFile != null)`). Both default off `identity.agentTokenFile` and `identity.forgeTokenFile`, which is what `allod/archetypes#17`'s `forgeAccess = false` opt-out clears.
- `forge-ssh-key` is gated on the machine's `forge_key` inventory fact, which the identity template does not touch. `allod/inventory` `flake.nix` declares `allod-dev.forge_key = "allod_vm"`, so **an `allod-dev` fixture with both token files nulled still declares `[ "forge-ssh-key" "ssh-host-key" ]`** — Git-over-SSH keeps working when the REST API credentials are gone, which is the honest shape.

A privacy microvm declares exactly `[ "ssh-host-key" ]`, because the privacy builder composes no Forge anything. A dev microvm reaches `[ "ssh-host-key" ]` only when both token files *and* `forge_key` are null. That is a real machine shape — `allod/inventory` already declares `forge_key = null` machines — but no dev machine has it today, so the fixture has to construct it.

**So `mkDevVm` takes `forgeKey ? machines.${name}.forge_key`.** It already takes `runtime ? machines.${name}.runtime`, `tokenFile ? identity.agentTokenFile` and `httpsTokenFile ? identity.forgeTokenFile` on exactly this pattern: a machine fact with an override the checks use to build a shape inventory does not contain. One more defaulted argument is what lets a fixture reach the no-Forge-at-all case without inventing a machine, and it changes no libvirt derivation because every real call omits it.

`forge-ssh-key` stays in this slice. Parent contract 9 explicitly moves the Forge `IdentityFile` off persistent home for microvm, and `allod/nexus` `scripts/forge-ssh-key` installing it imperatively today is the thing that path replaces. Leaving the name out would strand Git-over-SSH on the first microvm guest with no credential to point at.

Expose the set as a read-only internal option, `allod.microvm.credentialNames`, so the check reads a merged result rather than recomputing the builder's conditional. That is contract 4 of the runtime-selection slice applied here: a check that restates the generator proves nothing.

`machines.<name>.forge_key` is the same fact `vmFacts.<name>.forgeKey` already reads (`nix/vm-facts.nix`, `forgeKey = machine.forge_key or null`). Read it from `machines`, not from `vmFacts`, because `vmFacts` filters to non-hypervisors through its own trip-wire and the builders already read `machines.<name>` for `platform` and `runtime`.

### 3. Contract 7's rules apply to the declared set and to any supplied values

Assertions, all over the merged configuration:

- Every declared name is non-empty and at most 28 characters. Contract 7's bound is measured, not asserted by prose: pinned QEMU 10.1.5 `include/standard-headers/linux/qemu_fw_cfg.h:49-50` defines `FW_CFG_MAX_FILE_PATH 56`, and `system/vl.c:1163-1167` rejects `strlen(name) > FW_CFG_MAX_FILE_PATH - 1`. The name QEMU sees is the whole `opt/io.systemd.credentials/<cred>` string that `lib/runners/qemu.nix:156` builds; the prefix is 27 bytes, so `55 - 27 = 28`. A 29-character name makes QEMU exit at startup with `name too long (max. 55 char)` and `Restart=always` turns that into a restart loop with no framework diagnostic — the assertion is the only thing that reports it.
- **Names match `[A-Za-z0-9_-]+` and contain no `.`.** Two separate reasons, both measured. systemd's `credential_name_valid` (`src/shared/creds-util.c:49-53`) requires `filename_is_valid` and `fdname_is_valid`, so `/`, `:`, control bytes, `.` and `..` are out; a name failing it is **silently dropped** with only a `log_warning` in the guest journal, which is the worst possible failure shape. Independently, `,` and `=` would mis-split QEMU's own `name=…,file=…` option string. Forbidding `.` outright is what makes the rule cheap *and* closes the reserved-name hazard: every system credential systemd itself acts on is dotted — `ssh.authorized_keys.root`, `passwd.hashed-password.root`, `system.machine_id`, `tmpfiles.extra`, `sysusers.extra`, `fstab.extra`, `systemd.extra-unit.*`, `network.*`, `udev.rules.*` — and a host that supplied one of those names would silently reconfigure the guest rather than deliver a file the materializer reads. A dotless closed set cannot collide with any of them.
- The two name rules above apply to the **union** of the declared set and the keys of a non-empty `config.microvm.credentialFiles`, not to the declared set alone. The declared set is four literals in one builder and a read-only option, so a violation of it is a code change and no fixture can reach one; the supplied map is the untrusted half and is where a bad name actually arrives. Applying the rules to both is one `lib.unique` and is what gives every name rule a reachable sabotage.
- `config.microvm.credentialFiles` is either `{}` or has exactly the declared name set as its keys. `{}` is the standalone shape contract 8b requires; anything else must agree with the guest's own declaration, and a superset or subset fails.
- Every value in a non-empty `config.microvm.credentialFiles` is a plain string (not a Nix path), absolute, normalized, not under `builtins.storeDir`, **and contains no comma**. `microvm.credentialFiles` is typed `attrsOf path` upstream (`nixos-modules/microvm/options.nix:1070-1071`), which accepts a Nix path literal and would copy it into the world-readable store on interpolation at `lib/runners/qemu.nix:156`. The shared generic validator covers the first four. The comma rule is this option's own, for the same reason the name rules forbid one: `qemu.nix:156` interpolates the value straight into QEMU's comma-delimited `name=opt/io.systemd.credentials/<name>,file=<path>` string with no escaping, so one comma splits it into two options and QEMU exits at startup under `Restart=always`. `allod/nexus`' `hostPlaintextRoot` regex (`nix/microvm/host.nix`) permits commas in every segment, so a host-integrated configuration can pass every host-side check and still produce this; nexus rejecting it at the root is recorded for the 8b slice, and the guest asserting it is not redundant with a check that does not exist yet.

One value rule has no reachable fixture and stays anyway. `types.path` is `pathWith { absolute = true; }` (nixpkgs `lib/types.nix:671-673`, check at `:710-729`), so a *relative* value is rejected by the option's own type with nixpkgs' generic error before any assertion runs. The absolute rule stays in the shared validator because the credential root — typed to accept anything string-like precisely so a named assertion can report it — does need it. The value-side sabotage simply carries no relative case, and the plan does not claim one.

Values are only ever supplied by fixtures in this slice; contract 8b is where a real host supplies them. That is not a reason to defer the value rules — the assertion is the guest's half of contract 17 and the fixtures are how it is proved.

One fact for the 8b slice, recorded here because this is where it was measured: the machine component of the required value is the `microvm.vms.<name>` **attribute name**, which is also the systemd instance name, not `networking.hostName`. `allod/nexus` `nix/microvm/host.nix` carries a contract-17 assertion over each `vm.guest.microvm.credentialFiles` entry — `assertion = value == "${vm.credentialDirectory}/${cred}"` — whose expected path derives from that attribute name, and the two coincide only by convention in today's fixtures. This slice's guest-side assertions never build that string, so nothing here depends on it.

### 4. One runtime root, and the consumer table is the contract

Every path below is `<root>/<name>` with `<root>` the option value. Nothing restates a literal.

| File | Owner | Mode | Consumed by |
|---|---|---|---|
| `<root>/ssh-host-key` | `root:root` | `0600` | sshd's `HostKey` |
| `<root>/forge-token` | `<user>:users` | `0600` | the `forge` CLI via `FORGE_TOKEN_FILE` |
| `<root>/netrc` | `root:users` | `0640` | the Nix daemon and any nix client's curl-based fetches, via nix.conf `netrc-file` |
| `<root>/git-credentials` | `root:users` | `0640` | a **read-only** `credential.helper` for root and the user — never `store --file=…` itself; see below |
| `<root>/forge-ssh-key` | `<user>:users` | `0600` | the Forge host's `IdentityFile` in the rendered `~/.ssh/config` |

`<root>` itself is `0711 root:root`: traversable so the user can open its own files by exact path, not listable, so the set of credentials a guest holds is not enumerable by an unprivileged process.

Five notes on this table.

- **One netrc, not three, and git is not one of its readers.** Libvirt writes `/etc/nix/netrc`, `/root/.netrc` and `/home/<user>/.netrc`. Only the first has a declared consumer: `nix.extraOptions` points nix.conf's `netrc-file` at it, which covers the daemon's curl-based transfers *and* an unprivileged client's own — `0640 root:users` is exactly what lets the dev user's `nix` process read the same file the daemon does. The two `$HOME/.netrc` files exist for `git+https://` flake inputs, on the belief that Nix fetches them through libgit2 and libgit2 reads the calling user's `~/.netrc`.

  **That belief does not match the pinned Nix, and the correction is measured rather than argued.** Nix 2.31.5 *shells out to the `git` binary* for `git+https` network operations: a PATH shim recorded `git ls-remote --symref <url>`, then `git -C <cache> --git-dir . fetch <url>`, then `symbolic-ref`, all as child processes. `netrc` strings appear in `libnixstore`, not in `libnixfetchers`. So a `git+https://` flake fetch resolves its credentials through **git's own credential chain** — the `/etc/gitconfig` and per-user `git/config` helpers this contract rewires below — and not through anything nix reads out of a netrc file. `allod/vm` `modules/guest-base.nix` already sets a `credential.helper` for that reason.

  What follows is narrow, and the plan claims nothing wider. It does **not** claim `~/.netrc` has stopped working: git's HTTP transport can still consult it through curl, and that path was not tested. It claims only that a *user* netrc is unnecessary on a microvm guest, because every credential a user-run `git+https://` fetch needs already arrives through the rewired helper, and the daemon-side netrc has its own path and its own reader. Contract 9's "user netrc path if still needed" is answered: it is not needed. This supersedes the libgit2 rationale wherever it is still recorded.
- **`git-credentials` is `0640 root:users`, and that is not a new disclosure.** Today the dev user already holds the same Forge HTTPS password in `/home/<user>/.netrc` mode `0600`. Making the credential-store file group-readable changes no posture; it removes a second copy.
- **A file two principals share cannot be wired to a helper that writes, and `store` writes.** This is the note the `store --file=` design died on. Pinned git 2.51.2's `git-credential-store.adoc` documents `--file=<path>` as *"Use `<path>` to lookup **and store** credentials. The file will have its filesystem permissions set to prevent other users on the system from reading it"* — one helper for both directions, and a deliberate permissions reset on write. Git invokes that write path on its own: `git credential approve`, which git runs after a successful authentication, calls `store`, and `git credential reject` calls `erase`. So the failure needs no operator mistake. A root-run `nix flake update` over `git+https` authenticates, git calls `approve`, and `git-credential-store` rewrites `<root>/git-credentials` with permissions that exclude the `users` group — the unprivileged user's next fetch finds a file it cannot read, on a machine where nothing failed and nothing logged. The mirror-image failure is no better: the user's own `approve` cannot write, because the helper takes a lock file beside the credential file and `<root>` is `0711 root:root`, so an unprivileged process cannot create one there. Contract 6's materializer is the only writer this file may have; the host is the source of truth for its contents and rewriting it from inside the guest is a second inventory of the same fact (`architecture.md` principle 8).

  **So both files name a read-only helper instead**, generated into the store by this slice, referenced by absolute path, and shared by root and the user:

  ```sh
  #!/bin/sh
  # allod-microvm-credential-helper
  if [ "${1-}" = get ]; then
    exec <git>/bin/git credential-store --file=<root>/git-credentials get
  fi
  cat > /dev/null   # drain the request git wrote to our stdin
  exit 0
  ```

  Three mechanical points, all measured on the pinned documentation rather than assumed. Git executes an *absolute-path* helper string verbatim and appends the operation as an argument — `gitcredentials.adoc`'s rule 2, *"if the helper string begins with an absolute path, the verbatim helper string becomes the command"* — so a store path needs no `!` prefix and no `git credential-` prepending. Only `get` is delegated, so the only `git-credential-store` invocation that ever runs is a lookup, which opens the file read-only and takes no lock. Everything else drains stdin and exits 0: git writes the credential description to the helper's stdin, so a helper that exits without reading risks git reporting a write failure instead of proceeding, and `cat > /dev/null` costs one line to make the no-op silent.

  **What this deliberately gives up.** `git credential reject` no longer removes a bad credential, so a stale Forge password stays until the host delivers a new one and the machine reboots. That is the correct direction for a guest whose credentials are host-owned and per-boot: the alternative is a guest that can delete key material the host is authoritative for. Nothing else changes — `fill` still finds the credential, so every consumer contract 4 lists keeps working.
- **`forge-https-token` is delivered once and derived twice.** The host supplies one credential-store line; the Forge unit derives both the netrc and the credential-store file from it, exactly as `modules/netrc.nix`'s `system.activationScripts.netrc` derives netrc from `/root/.git-credentials` today. Two host credentials for one secret would be a second inventory of the same fact.
- **`git-credentials` has two readers and therefore two config files to fix, and pointing only one of them at it is a silent leak.** This is the note the rest of this section is about.

**The Git helper list is reset, not replaced, and it is reset in both files.**

`credential.helper` is multi-valued and *accumulates* across git's config files in read order — system, then user, then repository. Replacing one file's value does not remove another file's. Git's own documentation (pinned git 2.51.2, `gitcredentials.adoc:199-200`) states the escape hatch: *"If `credential.helper` is configured to the empty string, this resets the helper list to empty (so you may override a helper set by a lower-priority config file by configuring the empty-string helper, followed by whatever set of helpers you would like)."*

Both Allod files set a bare `store`, whose default file is `~/.git-credentials` — a **persistent** path on a microvm guest, and the one contract 9 forbids:

- `allod/vm` `modules/guest-base.nix` writes `environment.etc."gitconfig".text` containing `[credential]` / `helper = store`. Root reads this and nothing else.
- `allod/archetypes` `modules/home-shared.nix` sets `programs.git.settings.credential.helper = "store"`, rendered into the dev user's `git/config`. The user reads *both* files, and this one is read later.

So each file gets the reset plus the read-only helper, in that order:

- `/etc/gitconfig`: append with `lib.mkAfter` on `environment.etc."gitconfig".text`, which is `types.lines`. The appended block is a `[credential]` section header, then an empty `helper =`, then `helper = <store path>/bin/allod-microvm-credential-helper`. Not `mkForce`: forcing a `lines` option discards every other definition, and the reset is precisely the mechanism that makes discarding unnecessary. Acceptance test 18 records the same hazard for `nix.extraOptions`, where the original draft's defence against it did not work.

  **The block opens its own `[credential]` header, and that one line is load-bearing.** A bare `helper =` appended to a `lines` option belongs to whatever section precedes it in the *concatenated* file, not to the section the author had in mind. Today that is `allod/vm` `guest-base.nix`'s own `[credential]`, so the reset lands correctly and test 19 passes — but any third `environment.etc."gitconfig".text` definition arriving between the order-1000 block and this order-1500 one silently re-homes both appended lines into that definition's trailing section. Git would then read `<othersection>.helper`, the guest would keep its bare `store` helper pointed at persistent `~/.git-credentials`, and every line-oriented needle in test 19 would still match. Emitting the header closes the whole class for one line and costs nothing: a repeated `[credential]` section is legal git config.
- The user's `git/config`: give `modules/home-shared.nix` a `credentialHelper ? null` argument — the helper's absolute store path, not the credential root, because the module has no other use for the root and building the helper string in two places is the duplication contract 1 exists to prevent — and set `credential.helper = [ "" "<store path>/bin/allod-microvm-credential-helper" ]` when it is non-null, keeping today's `"store"` when it is null. Home Manager's `gitIniType` is `attrsOf (either multipleType sectionType)` with `multipleType = either primitiveType (listOf primitiveType)`, so a list renders one `helper = ` line per element, in order. No `mkForce`: this is the only definition of that attribute, and forcing it would be forcing our own value.

The two files render the same two settings **differently**, which is a trap for any check that greps for one string. `environment.etc."gitconfig".text` is raw text, so it renders `helper = /nix/store/…-allod-microvm-credential-helper/bin/allod-microvm-credential-helper` unquoted. Home Manager renders through `lib.generators.toGitINI`, whose `mkValueString` quotes every string, so the same setting renders `helper = "/nix/store/…"` and the reset renders `helper = ""`. Git strips the quotes on read and both mean the same thing; a needle built for one file does not match the other. Acceptance test 19 reads each file in its own form.

**Quoting is not merely cosmetic here, and it is why the helper takes no arguments.** Git appends the operation and hands the whole string to the shell (`gitcredentials.adoc`: *"The resulting command then has an 'operation' argument appended to it … and the result is executed by the shell"*), so the quotes Home Manager adds are stripped by git's own config reader before the shell ever sees the string, and a bare store path survives both renderings identically. Keeping every parameter — the credential file path — *inside* the generated script rather than on the helper line is what makes that true: an argument-carrying helper string renders differently in the two files in a way that git's config parser, not the shell, has to get right, and there is no reason to depend on that.

Root needs no user-side reset because Home Manager manages only the dev user; `/etc/gitconfig` is the whole of root's Git configuration.

**The Nix netrc path is appended for the same reason, by the same mechanism.** `allod/vm` `modules/guest-base.nix` defines `nix.extraOptions = "netrc-file = /etc/nix/netrc"`, and `nix.extraOptions` is `types.lines`. Append `netrc-file = <root>/netrc` with `lib.mkAfter`; do not `mkForce` the option. Nix takes the last occurrence of a repeated setting, so appending is sufficient, and it leaves every inherited line intact instead of discarding an unknown set of them. Acceptance test 18 carries the full argument, including why `nix.settings.netrc-file` does not work here and what the preservation control has to prove.

### 5. Contract 14 is a property of the table, and is asserted as one

No path in the table above may be equal to, or nested under, any `mountPoint` in the merged `config.microvm.volumes`. Assert it over the merged volumes list rather than over the required set the volumes module declares, so a profile that adds a volume at `/run/allod` is caught. Assert it over the whole table, not over the root alone: a root outside the volumes with one consumer path re-anchored elsewhere is the failure this is for.

### 6. Two materializer boot services, no optional branch in either

**Two** oneshot units with `RemainAfterExit = true`, not one, and the split is a correctness requirement rather than tidiness:

| Unit | Composed when | Owns | Required by |
|---|---|---|---|
| `allod-microvm-host-key.service` | always | `ssh-host-key` only, plus `<root>` itself | sshd, the Forge unit, and `sysinit-reactivation.target` |
| `allod-microvm-forge-credentials.service` | the machine declares at least one Forge name | `forge-token`, `forge-https-token`, `forge-ssh-key` and every file derived from them | `sysinit-reactivation.target`, and nothing else |

**"Required by" means every inbound requirement edge, including the ones this plan creates itself.** `requiredBy = [ "sysinit-reactivation.target" ]` is a real `Requires=` edge from that target onto both units — the plan asks for it deliberately, and test 13 asserts the symlink that carries it. Saying "nothing requires the Forge unit" while asking for that edge two paragraphs later is the plan contradicting itself, and it would make test 14's exhaustive negative unwritable. The honest statement, used everywhere below: **no credential consumer and no boot-path unit requires the Forge unit; `sysinit-reactivation.target` does, by design, and it is the only inbound requirement edge that may exist.**

**Why one unit is wrong.** Validation is per credential and any failure is fatal to the unit (below), and sshd `Requires=` the unit that carries the host key. Put all four credentials in one unit and a missing, empty or malformed *Forge* credential — every one of them optional, every one of them supplied by a host-side path this repo cannot see — stops the host key from ever being published, so sshd never starts and the guest is unreachable. The credential whose absence must stop sshd is the host key; nothing else is. Splitting the unit is what makes "fail visibly" and "stay reachable for repair" both true, and it costs one extra unit.

The failure the split accepts is the correct one: a broken Forge credential leaves the guest up, reachable, with `allod-microvm-forge-credentials.service` failed in `systemctl --failed` and Git, Nix-over-HTTPS and the `forge` CLI broken at the point of use. That is louder in the place it matters and quieter in the place it does not.

**What differs between the two units, and nothing else does.** Both use the same skeleton, the same fail-closed absolute `LoadCredential=` form, the same per-credential validation, the same atomic write idiom, and the same ordering block. The differences are exactly four:

- Their `LoadCredential=` sets are **disjoint**, and their union is exactly `config.allod.microvm.credentialNames`.
- The host-key unit creates `<root>`; the Forge unit does not, and carries `Requires=`/`After=` on the host-key unit so the directory has exactly one creator. That dependency runs the harmless direction — a failed host key already means sshd is down, so failing the Forge unit with it strands nothing extra. The forbidden edge is sshd depending on the Forge unit, not the Forge unit depending on the host key.
- The Forge unit also carries `After=systemd-sysusers.service`, because it writes files owned by the dev user and `install -o <user>` needs that user to exist. Under classic activation the user is already there; under `systemd.sysusers.enable` or `services.userborn.enable` it is created by a unit that is itself only `Before=sysinit.target`, so without this edge two `DefaultDependencies=no` units race. One `After=` covers both paths: pinned nixpkgs gives `userborn.service` `aliases = [ "systemd-sysusers.service" ]` (`nixos/modules/services/system/userborn.nix`), so the ordering resolves to whichever exists, and to nothing when neither does. The host-key unit writes `root:root` only and needs no such edge.
- Only the host-key unit carries the explicit `Before=` on sshd and is named in sshd's `Requires=`.

**Where the credential already is by the time these units run.** Measured on pinned systemd 258.7. PID 1 calls `kmod_setup()` (`src/core/main.c:3186`), which explicitly loads `qemu_fw_cfg` under KVM/QEMU with the comment *"would be loaded by udev later, but we want to import credentials from it super early"* (`src/core/kmod-setup.c:144-145`), and only then `initialize_runtime()` → `import_credentials()` (`main.c:3313`, `main.c:2453`). That reads `/sys/firmware/qemu_fw_cfg/by_name/opt/io.systemd.credentials` (`src/core/import-creds.c:375`) into `/run/credentials/@system` (`src/shared/creds-util.h:33`), files `0400 root:root` in a `0700 root:root` non-swappable tmpfs, then remounts it read-only.

This happens in the **initrd's** PID 1, because these guests run systemd-in-initrd: `microvm.optimize.enable` defaults true and `nixos-modules/microvm/optimization.nix:16-31` sets `boot.initrd.systemd.enable` for QEMU. It then survives switch-root — `src/shared/switch-root.c:38` carries `{ "/run/credentials", MS_BIND|MS_REC, 0 /* skip! */ }, /* Credential mounts should survive */` — and the real-root manager re-execs with `execv`, inheriting `$CREDENTIALS_DIRECTORY`, so `import_credentials()` short-circuits rather than re-importing.

**This corrects why contract 5 matters.** `boot.initrd.kernelModules = [ "qemu_fw_cfg" ]` is load-bearing because it puts the `.ko` in the initrd's `modulesClosure` at all — `qemu_fw_cfg` is a module, not built in, at kernel 6.12.93. Without it systemd's own early modprobe fails, `/sys/firmware/qemu_fw_cfg` never appears, and `import_credentials_qemu()` logs `"No credentials passed via fw_cfg."` and returns non-fatally. It is *not* about ordering against `systemd-modules-load.service`, which runs far too late to matter for this.

- **Input.** `LoadCredential = "<name>:/run/credentials/@system/<name>"`, for the host key on the host-key unit and for each declared Forge name on the Forge unit. **The absolute-path form is deliberate and is the first fail-closed gate.** `systemd.exec(5)` makes a bare `LoadCredential=<name>` **non-fatal** when the credential is absent — the unit starts and the file simply is not there — whereas an absolute source path that cannot be read fails the unit with `EXIT_CREDENTIALS`. `ImportCredential=` is likewise soft-fail. So the bare form would let a materializer run to completion having written nothing, and sshd would fail two units away from the cause. Each script then reads `$CREDENTIALS_DIRECTORY/<name>`, which for a system service is `/run/credentials/<unit name>/<name>`, mode `0400` — a different directory per unit, which is why the two `LoadCredential=` sets have to be disjoint rather than merely correct in aggregate.
- **Nothing in the guest references a host path.** `microvm.credentialFiles` values exist only in QEMU's argv on the host; no guest-side code may name `/run/allod/microvm/...`.
- **Script validation is the second gate, and is still mandatory.** systemd's gate proves the bytes arrived; it says nothing about their shape. The host launcher additionally refuses a missing, non-regular or empty *source* before QEMU starts — the per-credential `test -e` / `test -f` / `test -s` guards it emits over `credentialSources` in `allod/nexus` `nix/microvm/launcher.nix` — but the guest cannot rely on that either: upstream's `microvm -r <name>` execs the runner with no unit and no preparation, which `allod/nexus` `docs/microvm-host.md:44-48` documents as unsupported but reachable.
- **Validation, per credential, before any write.** The file exists, is a regular file, and is non-empty. `ssh-host-key` parses as an OpenSSH private key (`ssh-keygen -y -f` succeeds). `forge-https-token` yields exactly one non-empty line matching the `https://user:pass@host` shape — reuse the `sed -n 's|^https://…|machine \3 login \1 password \2|p'` expression in `modules/netrc.nix`, which already encodes it. `forge-ssh-key` parses as an OpenSSH private key. `forge-token` is a single non-empty line with no whitespace.
- **No skip branch.** Any failure is `exit 1` with a message naming the credential. There is no counterpart to the `"$credential missing or empty, skipping (expected during provisioning)"` branch `modules/netrc.nix` opens its activation script with, and the check asserts that string is *absent* from both microvm materializers while remaining present in the libvirt activation script.
- **Atomic writes, and `install` alone is not one.** `install(1)` truncates the destination in place; it does not write-and-rename. The idiom is `install -m <mode> -o <owner> -g <group> <src> <dir>/.<name>.tmp && mv -T <dir>/.<name>.tmp <dir>/<name>` — the temporary must be in the destination directory so the rename is a same-filesystem `rename(2)`, and the leading dot keeps a partial file out of a `<root>` listing. `allod/nexus` `nix/microvm/launcher.nix` establishes the same *principle* on the host side and is worth reading for it, but it is not the same implementation: the launcher stages an entire fresh directory (`stage=…/active/.stage-<name>`, `install`ing every credential into it) and publishes that whole directory once with `mv -T -- "$stage" <credentialDirectory>`. Per-file staging is chosen here because the two guest units publish into a directory the other one is also writing, so there is no single moment either could rename the whole thing. What both share, and what the check enforces, is that no consumer ever observes a partially written file. Either way, follow it rather than the weaker form `modules/netrc.nix` ends on — its three closing `install -o … -g … -m …` lines, which write each destination in place.
- **Ordering: `Before=sysinit.target` with `DefaultDependencies=no`.** This is the single ordering that precedes *every* normal service, every socket unit, `systemd-user-sessions.service`, `user@.service` and `sshd.service`, because systemd gives all of them a default `After=sysinit.target` (`systemd.service(5)`, `systemd.socket(5)`). Enumerating consumers instead — `before = [ "sshd.service" "nix-daemon.service" … ]` — is a list that goes stale the moment a profile adds a reader, which on a machine whose whole point is composed profiles is not a hypothetical. The skeleton is the one `nixos/modules/security/wrappers/default.nix:305-320` already uses for the same problem, plus the reactivation edges:

  ```nix
  wantedBy = [ "sysinit.target" ];
  requiredBy = [ "sysinit-reactivation.target" ];
  before = [ "sysinit.target" "sysinit-reactivation.target" "shutdown.target" ];
  conflicts = [ "shutdown.target" ];
  unitConfig.DefaultDependencies = false;
  serviceConfig.Type = "oneshot";
  serviceConfig.RemainAfterExit = true;
  ```

  **`requiredBy` alone is not the reactivation wiring, and getting this wrong is a rebuild-time race.** `switch-to-configuration` starts `sysinit-reactivation.target` to re-run changed early units in the right order relative to each other; `Requires=` creates the pull but no ordering at all. Pinned `nixos/modules/services/system/userborn.nix` — the precedent this plan cites — carries `"sysinit-reactivation.target"` in **both** `requiredBy` and `before`, and the `before` half is the load-bearing one. Without it, a rebuild that changes a materializer can reactivate a consumer before the materializer has rewritten the file, which produces a running guest holding stale credentials and no error anywhere. Both units carry both edges.

  **`wantedBy = [ "sysinit.target" ]` is the Forge unit's only start path.** Nothing `Requires=` that unit, by design, so this one list entry is the entire reason it ever runs at boot. Delete it and the guest is not failed, has nothing in `systemctl --failed`, and is simply missing every Forge credential — precisely the "working sshd, stale or absent credential, worse because it looks fine" failure the Risk Assessment names. The host-key unit is partly shielded from the same slip because sshd `Requires=` it; the Forge unit is shielded by nothing at all.

  **The symlink is what activates the unit, and it is the artifact a check must read — but the two options do not render alike.** Pinned `nixos/lib/systemd-lib.nix` emits `.wants`, `.upholds` and `.requires` symlinks from `wantedBy`, `upheldBy` and `requiredBy` — its own comment is *"Create .wants, .upholds and .requires symlinks from the wantedBy, upheldBy and requiredBy options"* — through `generateUnits`, into the tree that lands at `config.environment.etc."systemd/system".source`. On top of that, and **only for `wantedBy`**, `commonUnitText` in the same file appends an `[Install]` section to the unit's own fragment:

  ```nix
  optionalString (def.wantedBy != [ ]) ''

    [Install]
    WantedBy=${concatStringsSep " " def.wantedBy}
  '';
  ```

  So: `wantedBy` produces **both** an `[Install] WantedBy=` line in the materializer's own fragment and a `sysinit.target.wants/` symlink; `requiredBy` produces **only** the `sysinit-reactivation.target.requires/` symlink, and neither materializer's fragment nor the target's ever gains a `Requires=` line naming them.

  The symlink is nonetheless the assertion, for both. `[Install]` is instruction for `systemctl enable`, and a NixOS unit is immutable — `/etc/systemd/system` is a store symlink tree that `generateUnits` populates itself, so nothing on the running guest ever acts on that metadata. A fragment needle on `WantedBy=` would therefore stay green with the `.wants` symlink missing, which is exactly the silent-no-start failure this edge exists to prevent. Acceptance test 13 asserts the four symlinks for that reason, and deliberately keeps `WantedBy=` off its fragment needle list so the `wantedBy = []` sabotage loses the symlink needle alone.

  Keep an explicit `before = [ "sshd.service" ]` on the **host-key unit only**: it costs one list entry and survives someone later setting `DefaultDependencies=no` on sshd.

- **sshd's real dependency, and nothing else's.** `systemd.services.sshd.requires` gains `allod-microvm-host-key.service`, so a failed host-key materializer leaves sshd unstarted with a dependency failure rather than started against a missing file — contract 11's stated outcome, made a real edge rather than a coincidence. Two constraints on how that is written:

  - **No consumer `Requires=` the Forge unit, and `sysinit-reactivation.target` is the single deliberate exception.** Not `nix-daemon.service`, not `sshd`, not `sshd@`, not `sshd.socket`, not a user session, not a `home-manager-<user>.service`. Ordering from `Before=sysinit.target` is all those consumers get and all they need; a dependency would re-create exactly the stranding the split exists to prevent. The reactivation edge is the exception because it points the other way in blast-radius terms: it fires only during `switch-to-configuration`, on a machine that is already up, and its job is to stop a rebuild from leaving stale credentials behind. Test 14 enumerates the inbound edges and allows exactly two — the boot `.wants` edge and the reactivation `.requires` edge — rather than asserting an empty set it would immediately violate.
  - **Follow sshd's own conditional shape, and gate exactly what nixpkgs gates.** Measured on pinned `nixos/modules/services/networking/ssh/sshd.nix`: `systemd.services.sshd` is defined under `lib.mkIf (!cfg.startWhenNeeded)` and `systemd.sockets.sshd` under `lib.mkIf cfg.startWhenNeeded`, but `systemd.services."sshd@"` is defined **unconditionally** inside the module's `mkIf cfg.enable` — it is rendered on *both* branches. So only two of the three are branch-gated:

    - `systemd.services.sshd.requires` must be gated on `!config.services.openssh.startWhenNeeded`. This gate is mandatory and stays. An unconditional definition would materialize an `sshd.service` on the socket branch carrying a `Requires=` and no `ExecStart=`, which systemd refuses to load outright — a stray broken unit where nixpkgs deliberately renders none.
    - `systemd.sockets.sshd.requires` is gated the other way, on `startWhenNeeded`, for the same reason.
    - `systemd.services."sshd@".requires` needs **no** `startWhenNeeded` gate, because the template exists on both branches. On the default branch it is never instantiated, so the edge is inert; on the socket branch it is what makes each accepted connection wait for the host key.

    All three sit under the same `config.services.openssh.enable` condition nixpkgs' own definitions do. That is the general form of the same hazard: any of these edges written outside it would materialize an `ExecStart`-less unit on a guest with sshd disabled, which is the stray unit this bullet exists to avoid.

    `startWhenNeeded` defaults false and nothing in Allod sets it, so the socket branch is unreachable on any machine today; the wiring exists so a profile that flips the option neither loses the guarantee nor gains a stray unit. **It is reachable in a fixture, though, and both off-default branches carry one** — test 14 builds `startWhenNeeded = true` and `services.openssh.enable = false` on the privacy builder. Those fixtures do not test nixpkgs' branching, which is given; they test whether *these three edges* were written under the condition this bullet specifies, which is the only thing that can go wrong here and is invisible on the default branch.

    **No fixture may assert the absence of an `sshd@` attribute on the default branch** — nixpkgs puts one there, so that assertion fails on a correct implementation. The absence assertions in test 13 are about the *Forge materializer* attribute, which genuinely is composed conditionally; the absence assertions in test 14 are on the two branches where nixpkgs itself renders nothing.

- **Not `systemd-tmpfiles`.** The `f^` line type reads a credential into a file with declared owner and mode and is the idiomatic pinned-nixpkgs route — `nixos/modules/services/networking/ssh/sshd.nix:692-709` uses it for root's `authorized_keys`. It is rejected here for three concrete reasons, recorded so a later SIMPLIFY sweep does not reopen it: `systemd-tmpfiles-setup.service` imports a fixed credential allowlist (`ImportCredential=tmpfiles.*`, `ssh.authorized_keys.root`, …), so any other name is *silently skipped* unless a drop-in is added in three separate units nixpkgs keeps in sync; `f` never rewrites an existing file, and `f+` truncates in place rather than renaming; and the tmpfiles specifier table has no `%d`, so the `^` modifier is the only route and it cannot express per-credential validation at all.
- **The directory.** The host-key unit's script creates `<root>` itself with the declared mode, and it is the only creator. A `systemd.tmpfiles` rule would be a second declaration of the same path in a different language, ordered by a different unit.

### 7. sshd is pointed at the delivered key, and cannot mint one

Measured against pinned nixpkgs `nixos/modules/services/networking/ssh/sshd.nix`:

- Line 846 builds the `HostKey` directives solely from `cfg.hostKeys`. With `hostKeys = []` no `HostKey` line is rendered, and sshd then falls back to its **compiled-in** defaults. Measured directly against the pinned binary: `sshd -G -T -C lport=22 -f /dev/null` reports `hostkey /etc/ssh/ssh_host_rsa_key`, `…ssh_host_ecdsa_key`, `…ssh_host_ed25519_key`. So `hostKeys = []` on its own is not a safe state — it silently re-adopts any stale key sitting at one of those three paths, and exits with `no hostkeys available` if none does. An explicit `HostKey` directive is what discards the defaults, and it is mandatory, not merely convenient.
- Lines 779-802 define `services.sshd-keygen` with `ConditionFileNotEmpty = map (k: "|!${k.path}") cfg.hostKeys` and `script = concatMapStrings … cfg.hostKeys`. With `hostKeys = []` both collapse: no condition line is emitted, and `script = ""` means nixpkgs emits no `ExecStart` at all (`nixos/lib/systemd-unit-options.nix:501-508`). systemd then **refuses to load** the unit — `src/core/service.c:686-691`, `"Service has no ExecStart=, ExecStop=, or SuccessAction=. Refusing."` Because both sshd units only `Wants=` it (not `Requires=`), sshd still starts, so nothing is minted; but the guest boots with a load error in its journal on every boot. Mask it instead: `systemd.services.sshd-keygen.enable = false` renders a `/dev/null` symlink (`nixos/lib/systemd-lib.nix:74,99`), which is a real mask, is quiet, and is a stronger statement than an empty script.
- Keeping `hostKeys` pointed at the runtime path instead of emptying it is the option that must not be taken. The `ConditionFileNotEmpty=|!<path>` guard skips the unit only while the file is non-empty, so the first boot where the credential fails to arrive is exactly the boot where `sshd-keygen` mints a fresh ed25519 key at the runtime path and sshd comes up with an unknown identity — silently. That is the precise failure contract 11 exists to forbid.
- Line 826 defines the module's own `extraConfig` contribution with `lib.mkOrder 0`, so a plain `services.openssh.extraConfig` definition is appended after it. `services.openssh.settings.HostKey` also works for a single key, but throws on a list (`sshd.nix:69-71`), so `extraConfig` is the form that stays correct if a second key is ever added.

So for microvm: `services.openssh.hostKeys` defined as `[]` lexically inside `sharedModules` (not `mkForce`; the definition is the builder's own and is simply omitted for microvm), plus `services.openssh.extraConfig = "HostKey <root>/ssh-host-key"`, plus `systemd.services.sshd-keygen.enable = false`.

sshd refuses a host key file that is group- or world-accessible; the table's `0600 root:root` satisfies it. The public half is not delivered: sshd derives it from the private key, and the pinned module's build-time `sshd -G -T` check does not open host key files — proved today by `/etc/ssh/<name>`, which does not exist at build time either. **That same check also exits 0 with no host key configured at all**, so it is not a guard against the empty-`HostKey` state above; acceptance test 16 is the only thing that is.

### 8. No age, and it costs one omission rather than one removal

`agenix.nixosModules.default` stays in `sharedModules` for microvm. Its entire `config` is `mkIf (cfg.secrets != {})` (agenix `modules/age.nix:271`), including the `identityPaths != []` assertion at line 275, so a guest that declares no secrets composes no activation script, no systemd unit, and no assertion. Keeping the module imported keeps `config.age.secrets` a *declared* option, which is what lets the check assert `== {}` instead of probing for an absent attribute, and keeps `credential-profiles`' `machineConfigurations.<m>.config.age.secrets` read total.

`age.identityPaths` is not defined for microvm. Its default (agenix `modules/age.nix:241-254`) is derived from `config.services.openssh.hostKeys`, which contract 7 above makes `[]`, so the option resolves to `[]` with no definition of ours. Assert `config.age.identityPaths == []`.

What the builder omits for microvm, all in `mkDevVm` and `sharedModules`: the `age.secrets.forgejo-https-token` attrset guarded by `lib.optionalAttrs (httpsTokenFile != null)`, the `lib.optional (tokenFile != null) ./modules/agent-forgejo-token.nix` entry, the `./modules/netrc.nix` entry, and the `services.openssh.hostKeys` / `age.identityPaths` pair `sharedModules` sets to `/etc/ssh/${name}`.

`(mkGithubCredentialModule name)` stays composed, and a microvm machine with a non-empty GitHub credential target set **fails evaluation** with a named diagnostic pointing at the follow-up. The public template declares none, so deriving credential names for them now would be untestable speculative generality; refusing them is honest and is one assertion.

This is a deviation from parent contract 9, which requires *any* registered GitHub target to be configured to runtime files, and it is recorded as contradiction 5 with the follow-up named there. The diagnostic must point at that follow-up by name, not merely say "unsupported": a private inventory that registers a GitHub target for a microvm machine gets this failure, and the message is the only thing telling its operator whether they hit a bug or a deliberate boundary.

**Naming the route the fixture takes, because today there is none.** `mkGithubCredentialModule name` closes over the flake's own `secrets` input, and `modules/github-credentials.nix` derives `targets = secrets.lib.githubCredentialTargets.${machineName} or []` for itself. The public template pins that attrset empty and no `mkDevVm` argument reaches it, so "a fixture that injects a target" cannot be written without replacing the `secrets` input wholesale. Thread it instead, on the exact `forgeKey` pattern of contract 2: `mkDevVm` gains `githubCredentialTargets ? secrets.lib.githubCredentialTargets.${name} or []`, `mkGithubCredentialModule` takes that list rather than deriving one, and `modules/github-credentials.nix` takes a `targets` argument in place of its internal lookup. The same value feeds the microvm refusal assertion, so an injected target reaches both consumers and the two cannot disagree. Every real call omits the argument and its default is the expression the module reads today, so the four libvirt derivation paths are unchanged — which is what test 12's fixture then exercises.

### 9. The closure carries no key material

A generated check over the microvm guest's `system.build.toplevel` closure asserting it contains no `.age` file, no age or agenix identity, and no private key. The realistic failure this catches is a Nix path literal reaching an option that interpolates it — `microvm.credentialFiles` being `attrsOf path` is exactly that hazard, and `secrets.lib.*TokenFile` values are Nix paths into the `secrets` store path today.

Scan the *closure*, not the toplevel directory: an `age.secrets.<n>.file` reference lands as a store path in the activation script, whose closure includes the ciphertext.

**Three detectors, three positive controls, and one of the controls is not the others.** The check runs three independent predicates — a filename ending in `.age`, an age or rage identity in the file contents, and an OpenSSH private key header in the file contents — and each one has to be shown capable of rejecting. The libvirt dev machine's closure supplies exactly one of those proofs: it contains the two `.age` ciphertexts, which proves traversal happens and that the *filename* predicate fires, and nothing more. Both content predicates could be deleted outright and the check would stay green.

So add two synthetic canary files, built by the check itself, and assert each content detector rejects its own canary:

- a file containing the literal `AGE-SECRET-KEY-1` prefix followed by filler, for the age/rage identity detector;
- a file containing an `-----BEGIN OPENSSH PRIVATE KEY-----` header followed by filler, for the private-key detector.

They are strings in the check, not key material: no real secret and no generated key goes anywhere near a fixture. They are also not a second NixOS closure — building another full system to carry a canary is the memory cost this budget cannot pay. Run each predicate over the canary directly and require a rejection, then run all three over the microvm closure and require silence.

### 10. The libvirt path is byte-identical

Every change is gated on `runtime == "microvm"` outside the module system, so no libvirt machine's module list changes. Proved by derivation-path equality across the implementing commit, not by inspection.

### 11. No swap, because the credential root is ordinary `/run` and ordinary `/run` is swappable

Parent contract 8a says, in as many words, *"No microvm guest declares a swap device."* Nothing in this plan asserted it, and the omission is not cosmetic: this is the slice that decides where plaintext key material lands, so it is the slice that owes the assertion.

**The distinction is measured, and it is between two tmpfs mounts that look alike and are not.** systemd's own credential store is deliberately unswappable: `mount_credentials_fs()` (pinned systemd 258.7, `src/shared/mount-util.c:1758`) tries `tmpfs` with `noswap` first and says so — *"1. tmpfs if it supports noswap"*, falling back only when the kernel is older than 6.3. That is the mount behind `/run/credentials/@system`, where the delivered credential sits before either materializer touches it. `/run` itself gets no such treatment: `mount_setup()`'s table (`src/shared/mount-setup.c:98`) mounts it `"tmpfs", "/run", "tmpfs", "mode=0755" TMPFS_LIMITS_RUN, MS_NOSUID|MS_NODEV|MS_STRICTATIME` — no `noswap`, on either the SMACK or the plain branch.

So the moment the host-key materializer copies the key out of `/run/credentials/@system` and into `<root>`, the copy stops being pinned to RAM. The same is true of the materializers' own process memory while they validate a key, and of every consumer that reads one. If any profile ever enables swap on a microvm guest, an ed25519 host key and a Forge password become writable to a block device that outlives the boot — the exact failure the whole runtime-credential design exists to avoid, arriving through an option nothing in this plan looks at.

**So assert it, on every microvm configuration:** `config.swapDevices == []` and `config.zramSwap.enable == false`, one named diagnostic each. Both are unconditionally declared NixOS options, so both reads are total. No Allod repository sets either today — checked across every `.nix` file in the workspace — which is precisely why the assertion is cheap now and the only thing that will notice when a profile changes its mind.

`zramSwap` needs naming separately from `swapDevices` and is not redundant with it: it is the swap a profile is most likely to enable for apparently harmless reasons, it is compressed rather than encrypted, and it reaches `swapDevices` through the module's own internal definition rather than through the option a reviewer would think to check.

**Two things this contract does not do.** It does not give `<root>` its own `noswap` mount. That would be strictly stronger, and the pinned 6.12.93 kernel supports the option, but it makes `<root>` a mount point declared in one language and created in another — a second creator of the same path, which contract 6 forbids for exactly the reason it rejects `systemd-tmpfiles`, plus an ordering edge between the mount unit and the materializer that nothing in this slice can boot to verify. It is recorded here as the available upgrade for whoever revisits this with a guest they can boot, not adopted. And it does not prove the assertion holds on a running guest: **the nested-boot slice owes a `/proc/swaps` check on a booted microvm guest**, recorded here because this is where the requirement was derived. An evaluation assertion proves no configuration declares swap; only the running kernel proves none appeared.

## Agent Gates

None for implementation. Every acceptance test runs locally without host access or real credentials.

One gate on *consumption*: this change does not license enabling a machine on the microvm runtime. See the gate in Risk Assessment.

## Acceptance Tests

### The libvirt no-op

Taken across the implementing commit:

```sh
cd /path/to/archetypes
for m in allod-dev privacy-1 nexus installer; do
  nix eval --raw ".#nixosConfigurations.$m.config.system.build.toplevel.drvPath"
done
```

Must be byte-identical before and after:

| Machine | drvPath hash |
|---|---|
| `allod-dev` | `1lhcxfhy9iddfssp3df3r2zwkwnd3ilm` |
| `privacy-1` | `zzzaralwv032jdmbqc1q6ms9fm15w7yf` |
| `nexus` | `fad4pq24f8iavzr3q369alz8932jq9q3` |
| `installer` | `2yj5038jnsgzbv6ghhmyafs5kspn9jrg` |

Then the whole set:

```sh
nix flake check --print-build-logs
```

### Memory budget

`runtime-module-selection` already states its own constraint, in the comment above its `sabotages` list: fixtures are consumed as thunks one at a time because *"Holding them all live at once instead is an OOM kill on an 8 GB builder — measured, not feared"*, and even consumed one at a time they are most of what takes `nix flake check` from a ~4 GB peak to over 5 on a 7 GiB box with no swap.

**The budget is what shapes the fixture list below, not a footnote appended to it.** The original draft of this plan listed more than twenty full-system sabotages and then said what to do if the peak rose too far — which targets the cheap half (a handful of positive assertions that only read projections) and leaves the expensive half (every forced negative system) untouched. Forcing a system is what runs `config.assertions`, so a negative fixture cannot be made cheap; it can only be made rarer.

So the count comes down first, by moving what does not need a system out of the systems:

- **Table-test the pure validators exhaustively.** Contract 1 hoists the path validator and contract 3 the name validator into `flake.nix`'s top-level `let`. Every invalid *shape* — Nix path, relative, store-prefixed, trailing slash, `.`/`..` segment, `/run/allod` itself, outside `/run/allod`, empty name, 29 characters, `/`, `.`, `:`, `,` — is checked by calling the function on a string and comparing the returned message list. No `nixosSystem`, no forcing, effectively no memory. This is where exhaustiveness lives.
- **Keep one full-NixOS sabotage per assertion family, not per input.** The families are: the credential root is rejected; a supplied `credentialFiles` key is rejected; a supplied `credentialFiles` value is rejected; the key *set* disagrees with the declared set; a consumer path overlaps a persistent mount; agenix wiring is present; `hostKeys` is non-empty; a GitHub credential target exists; a generated unit was mutated. Each family's fixture proves the wiring — that the validator is actually called on the real merged value and its message reaches `config.assertions` — which is exactly what a pure-function table cannot prove and the only thing a system is needed for.
- **Load each surviving fixture up.** One bad `credentialFiles` map can carry several invalid keys *and* several invalid values at once; one map can be both missing a required key and carrying an extra one; the generated-unit mutations of tests 13 to 15 can share a fixture, since `checkSabotage` already takes a list of needles per fixture for precisely this reason. Combine wherever the needles stay individually pinned.

  **Exactly one mutation cannot join that shared fixture**: test 13's `wantedBy = []` sabotage, whose entire point is that every *other* needle stays green while one symlink disappears. Everything else added by this plan rides a fixture that is already forced, and the accounting is worth stating in full because the temptation is always to add one more thunk:

  | Mutation | Rides | New forced systems |
  |---|---|---|
  | Test 13's `wantedBy = []` | its own thunk — nothing else may be broken in it | +1 |
  | Test 5's moved credential root | the moved-image-root fixture already forced | −1 |
  | Test 13's credential-set mutation | the shared unit-mutation fixture | 0 |
  | Test 15's owner/mode mutation | the shared unit-mutation fixture | 0 |
  | Test 23's persistent-copy mutation | the shared unit-mutation fixture | 0 |
  | Test 10's swap declarations | test 10's existing agenix-wiring fixture | 0 |

  **Net: zero.** Three of those ride the shared unit-mutation fixture because `checkSabotage` already takes a list of needles per fixture and `missingDiagnostic` pins each needle to its own assertion, so one mutated system can violate several rules at once without any of them becoming unpinnable — that is the whole reason the shared fixture exists. Test 23's scanner mutation in particular needs no thunk of its own: it needs a unique persistent destination literal in an artifact that something already renders, and the shared fixture already renders every artifact it reads.

  Three additions in this plan are not NixOS systems at all and cost effectively nothing: test 19's nearest-section canary and the two validator tables are pure-function calls, and test 19's credential-helper behaviour check is a `runCommand` with `pkgs.git` and a fixture file. Test 14's two sshd-branch fixtures are the one genuine addition beyond the table — they are built on the privacy builder and read `config.systemd.units.<n>.unit`, so they render units without forcing `system.build.toplevel`, which `nix.md` records as the far cheaper of the two. Measure them like anything else.
- **Every new fixture is a thunk consumed exactly once by `checkSabotage`.**
- **Drive every archetype-independent family from the privacy builder**, which composes no dev user environment: the root validation, the name and value rules, and the contract 14 overlap rule. Reuse `asMicrovm` for every positive assertion rather than building a second dev fixture. The one assertion that cannot use `asMicrovm` — test 5, which needs a *non-default* credential root — reuses the moved-image-root fixture the check already forces rather than adding a dev system of its own.
- **Measure after each small batch, not after the list is complete.** `\time -v nix flake check`, maximum resident set size, recorded in the PR body. A batch that moves the peak more than ~5% stops and gets merged into an existing fixture before the next batch starts. Discovering the OOM after twenty fixtures means unpicking twenty fixtures.

### Contract assertions, each with a paired sabotage fixture

Every fixture case below is `{ name; fixture = _: …; needles = [ … ]; }` in `runtime-module-selection`'s existing `sabotages` list, pinned through `missingDiagnostic` against `config.assertions`, and verified by deletion: removing the assertion must make the fixture stop failing for its named reason.

**Verify it in the other direction too, and do that before writing the fixture.** For every assertion a sabotage claims to break, work out whether the mutated configuration still *satisfies* that assertion. Deletion-verification catches an assertion nothing exercises; it does not catch a sabotage aimed at the wrong assertion, because the fixture fails either way and the check goes green on a needle some *other* violation produced. This plan shipped one of those into three review passes — test 13's relocation mutation, which leaves disjointness and union both true and can only ever fail the per-unit set assertions — and the same shape has now been found five times across this arc, every time by a reviewer and never by the test. The rule that closes it is one sentence long: **a sabotage names the assertion it fails, and that assertion must be false under the mutation, not merely nearby.**

**0. The validator tables come first, and they carry the exhaustiveness.** Two pure-function tables, no `nixosSystem` anywhere in them:

- The path validator, over: a Nix path, a relative path, a store-prefixed path, a trailing slash, a doubled slash, a `.` segment, a `..` segment, `/run/allod` itself, a path outside `/run/allod`, and a valid path. Each case asserts the exact expected message list, not merely that it is non-empty.
- The name validator, over: empty, 28 characters (valid), 29 characters, `/`, `.`, `..`, `:`, `,`, `=`, a leading dash (valid), and a valid name. The `.` case asserts the reserved-system-credential wording specifically, so it cannot be satisfied by a generic "invalid character" message.

Tests 6, 7 and 8 below then each keep **one** system fixture whose only job is to prove the validator is wired to the real merged value and its message reaches `config.assertions`.

Positive, on `asMicrovm` and `asPrivacyMicrovm`:

1. A dev microvm's `config.allod.microvm.credentialNames` is exactly `[ "forge-https-token" "forge-ssh-key" "forge-token" "ssh-host-key" ]`; a privacy microvm's is exactly `[ "ssh-host-key" ]`.
2. **The two gates are independent, and it takes three fixtures to show it.** Built with `runtime-module-selection`'s own `devFixture`, which forwards arbitrary arguments to `mkDevVm`, so each case is `devFixture { runtime = "microvm"; identity = <dev identity> // { agentTokenFile = …; forgeTokenFile = …; }; forgeKey = …; }`. **Not** `dev-forge-opt-out`'s `withTokens`: these fixtures read `config.allod.microvm.credentialNames`, which exists only under microvm, and `withTokens` passes no `runtime` and lives in another check's `let`, so using it would mean either hoisting it across two checks or adding a second pass-through argument to it. Nulling the two *identity* fields rather than passing `mkDevVm`'s `tokenFile`/`httpsTokenFile` directly is what keeps the fixture on the production path: those arguments default to `identity.agentTokenFile` and `identity.forgeTokenFile`, and a nulled identity is the shape the secrets template produces under `forgeAccess = false`.
   - both token files null, `forge_key` left at inventory's `"allod_vm"` → `[ "forge-ssh-key" "ssh-host-key" ]`. This is the real `forgeAccess = false` shape for `allod-dev`, and it is **not** `[ "ssh-host-key" ]`: the identity template does not touch `forge_key`, so Git-over-SSH survives the opt-out.
   - both token files present, `forgeKey = null` → `[ "forge-https-token" "forge-token" "ssh-host-key" ]`.
   - both token files null and `forgeKey = null` → `[ "ssh-host-key" ]`. This is the only way to reach the no-Forge-at-all shape from a dev builder, and it is why `mkDevVm` takes the argument.

   A fixture that changed only the token files and expected `[ "ssh-host-key" ]` would be asserting a machine shape that cannot exist; that is what the first case exists to prevent recurring.
3. `config.age.secrets == {}` and `config.age.identityPaths == []` on both microvm fixtures.
4. `config.services.openssh.hostKeys == []` on both.
5. **A non-default `allod.microvm.guestCredentialRoot` moves every derived path — on the fixture that already exists, not a new one.** `runtime-module-selection` already pays for one fully forced dev system carrying a non-default `volumeImageRoot`: `movedRootFindings`, built by `withImageRoot`. Move `guestCredentialRoot` in that same fixture instead of forcing a second dev system for it. The two options are independent, so one fixture moving both roots proves both families exactly as well, and the check's own comment records that a forced fixture is not free on a 7 GiB box. Assert the moved root reaches every entry in the consumer table, both materializer scripts, the `HostKey` line, the appended `netrc-file` line, **both** Git config files, and the `IdentityFile` in the rendered `~/.ssh/config`. Restate the default literally in the check, for the reason the existing `expectedImageRoot` comment gives: a check that derived the default from the option it is checking could not notice the default moving.

Sabotage, each pinned to its own diagnostic:

6. **Credential root, one fixture.** The privacy builder with the root forced to a store-prefixed path. Needle anchored to `allod.microvm.guestCredentialRoot` by name, for the reason the `imageRootOption` comment already records: the same validator runs over several values, so an unanchored needle like "must be an absolute path" is satisfied by another reporter's message and stays green with this assertion deleted. Every other invalid root shape is a row in the table above.
7. **Credential names, one fixture.** `microvm.credentialFiles` forced to a map whose keys include a 29-character name, an empty name, a name containing `/`, and a name containing `.`, all at once; four needles on one fixture. The `.` needle must name the reserved-system-credential reason.

   **No collision case.** The original draft asked for "two names colliding after normalization" to match parent contract 7. There is no normalization step anywhere on this path to collide through, and Nix attribute keys are unique by construction, so the fixture cannot be written without inventing the operation it violates. See contradiction 3.
8. **Credential values and key set, one fixture each.** One map carrying a Nix path value, a store-prefixed value and a value containing a comma; one map that both omits a declared key and adds an extra one.

   **No relative-value case.** Contract 3 already establishes why: `microvm.credentialFiles` is `attrsOf path`, `types.path` is `pathWith { absolute = true; }`, and a relative value is rejected by the option's own type with nixpkgs' generic error before any assertion runs. Upstream's type error *is* the enforcement evidence for that rule on the value side. The absolute rule keeps its named assertion and its sabotage on `guestCredentialRoot`, where the permissive option type makes it reachable — that is test 6's table row.
9. A `microvm.volumes` entry forced to `mountPoint = "/run/allod"`, so a consumer path is nested under a persistent mount.
10. **Forbidden wiring, one fixture, four needles.** `age.secrets` forced non-empty; `age.identityPaths` forced non-empty; `swapDevices` forced to one entry; `zramSwap.enable` forced true. All four are "microvm must not have this" families over the same merged configuration, none of them interferes with another's diagnostic, and `missingDiagnostic` pins each needle to its own assertion — so they share one thunk rather than spending four. The two swap needles are contract 11's, and the assertion they pin is the only thing in this plan that keeps materialized plaintext out of a swap file.

    Also assert the positive side on both microvm fixtures: `config.swapDevices == []` and `config.zramSwap.enable == false`. Those are two option reads on fixtures that already exist, and without them a deleted assertion leaves no trace in a passing run.
11. `services.openssh.hostKeys` forced non-empty on a microvm fixture.
12. A microvm dev fixture built with `githubCredentialTargets` overridden to a non-empty list — the defaulted `mkDevVm` argument contract 8 adds, which exists so this fixture can be written at all.

### Generated-artifact checks

These are the tests this slice exists for, and none of them may be replaced by an option read.

13. **The two rendered materializer units, and the split itself.** Build `config.systemd.units."allod-microvm-host-key.service".unit` and `…"allod-microvm-forge-credentials.service".unit` and read both fragment files. Assert:
    - `allod-microvm-host-key.service` carries exactly one `LoadCredential=`, `ssh-host-key:/run/credentials/@system/ssh-host-key`, and the Forge unit carries exactly one per declared Forge name. The two sets are **disjoint** and their union is exactly `config.allod.microvm.credentialNames`.
    - Each carries `DefaultDependencies=no`, `RemainAfterExit=yes`, `Before=` naming both `sysinit.target` **and** `sysinit-reactivation.target`, and `Conflicts=shutdown.target`.
    - **The four pull symlinks**, read off `config.environment.etc."systemd/system".source` and not off any fragment: `sysinit.target.wants/allod-microvm-host-key.service`, `sysinit.target.wants/allod-microvm-forge-credentials.service`, `sysinit-reactivation.target.requires/allod-microvm-host-key.service`, and `sysinit-reactivation.target.requires/allod-microvm-forge-credentials.service`. Contract 6 gives the mechanism and the asymmetry between the two options: `requiredBy` renders *only* as a symlink, so `sysinit-reactivation.target`'s own unit file carries no `Requires=` line naming these units and an assertion looking for one fails a *correct* implementation; `wantedBy` renders as a symlink **and** as an `[Install] WantedBy=` line in the materializer's own fragment, which nothing on an immutable NixOS unit acts on. That is why the needle is on the symlink for both, and why this test keeps `WantedBy=` off its fragment-needle list: a fragment needle would survive the missing symlink, and the `wantedBy = []` sabotage below would then lose two needles instead of demonstrating that the symlink assertion is the one doing the work. The `sysinit.target.wants` pair is the only thing in this plan that asserts either materializer starts at boot at all, and for the Forge unit it is the only start path that exists.
    - Only the host-key unit's `Before=` names `sshd.service`. Only the Forge unit carries `After=systemd-sysusers.service` and `Requires=`/`After=` on the host-key unit.
    - For `asPrivacyMicrovm`, and for the dev fixture with no Forge names, `config.systemd.services` has **no** `allod-microvm-forge-credentials` attribute at all.

    Paired sabotages, sharing fixtures where the needles stay pinned: a fixture that drops the `before` lists loses the `sysinit.target`, `sysinit-reactivation.target` and `sshd.service` needles together; a fixture that uses the bare `LoadCredential=<name>` form loses the absolute-path needle, which is what defends the soft-fail distinction that is invisible in the option value; and a fixture that `mkForce`s the Forge unit's `wantedBy` to `[]` must lose the `sysinit.target.wants/allod-microvm-forge-credentials.service` needle **while every fragment-file needle stays green**, which is the whole reason that assertion reads the symlink tree. Contract 6 records why that stays true after the `[Install]` correction: `wantedBy` does put a `WantedBy=` line in the fragment, and this test deliberately does not have a needle on it.

    **The credential-set sabotage, and the three assertions it has to be aimed at correctly.** The original form of this fixture said "moving `ssh-host-key` onto the Forge unit must fail the disjointness and union assertions". It cannot, and that is a test that could never have failed for its stated reason. Work the mutation through: a true *relocation* leaves the host-key unit with `{}` and the Forge unit with all four names, so the two sets are still disjoint and their union is still exactly `credentialNames` — both named assertions stay true and the fixture would only ever have failed on the per-unit set assertions it did not name. Each of the three assertions has exactly one mutation class that falsifies it:

    - **Relocation or misassignment** — a declared name on the wrong unit — falsifies only the **exact per-unit set** assertions.
    - **Duplication** — the same name on both units — falsifies **disjointness**, and preserves the union.
    - **Removal or undeclared addition** — a name dropped from both units, or one appearing on a unit that `credentialNames` does not contain — falsifies the **union**, and, for removal, preserves disjointness.

    So all three go into **the shared unit-mutation fixture**, as one mutation of the two `LoadCredential=` sets carrying one needle per assertion: put `forge-token` on the host-key unit *as well as* the Forge unit (host-key's exact set is no longer `{ ssh-host-key }` → per-unit needle; the same name on both → disjointness needle), and drop `forge-ssh-key` from the Forge unit entirely (the union is now a proper subset of `credentialNames` → union needle). No new forced system, three assertions, each one genuinely false under the mutation. This is the sabotage for the split itself: a two-unit design whose fixtures cannot tell the units apart is worse than one unit, and a fixture that only appears to tell them apart is worse than both.
14. **sshd's real dependency edge, and the exhaustive set of edges that may point at either materializer.** In the rendered `sshd.service`, assert `Requires=` names `allod-microvm-host-key.service` and does **not** name `allod-microvm-forge-credentials.service`. Paired sabotage: dropping the `requires` definition loses the first needle.

    **The Forge unit's inbound edges are asserted as an exact set, not as an empty one.** Contract 6 records why an empty-set assertion would be wrong: `requiredBy = [ "sysinit-reactivation.target" ]` is itself an inbound requirement edge, so "no unit anywhere requires the Forge unit" fails on a correct implementation. Collect every inbound edge naming `allod-microvm-forge-credentials.service` from two places at once — a `Requires=`, `Requisite=`, `BindsTo=`, `PartOf=` or `Wants=` line in *any* rendered unit fragment, and *any* `*.wants`, `*.requires` or `*.upholds` symlink under `config.environment.etc."systemd/system".source` — and assert the collected set is exactly `{ sysinit.target.wants, sysinit-reactivation.target.requires }`. Anything else, whether a service, a socket, a template or a user unit, fails. Paired sabotage: adding the Forge unit to sshd's `requires` must produce a third member and fail.

    Enumerating both artifacts is what makes this exhaustive rather than a spot check: contract 6 has just established that these two edge kinds render into *different* files, so a scan of fragments alone would miss the symlink edges and a scan of symlinks alone would miss a `Requires=` line someone adds by hand.

    **Both sshd branches carry a fixture, and neither is a test of nixpkgs.** The earlier "a fixture that set the option would be testing nixpkgs" is wrong about what is under test: nixpkgs' branching is given, and what these fixtures measure is whether *Allod* put its three new edges under the correct condition. Contract 6 states the condition; these prove it. Both are built on the privacy builder and read `config.systemd.units.<n>.unit`, so they render units without forcing a toplevel.

    - `services.openssh.startWhenNeeded = true`: assert `sshd.socket`'s rendered unit carries `Requires=allod-microvm-host-key.service`, that `sshd@.service` carries it too, and that `config.systemd.services` has **no** `sshd` attribute — nixpkgs renders none on this branch, so an Allod definition written outside the `!startWhenNeeded` gate would materialize an `ExecStart`-less `sshd.service` that systemd refuses to load, which is precisely the stray unit contract 6 gates against.
    - `services.openssh.enable = false`: assert `config.systemd.services` has no `sshd` and no `sshd@` attribute and `config.systemd.sockets` has no `sshd` attribute — nixpkgs puts its whole configuration under `mkIf cfg.enable`, so any of the three edges written outside that condition shows up here as a unit with a `Requires=` and nothing to start.

    On the default branch only, **no fixture may assert that an `sshd@` attribute is absent** — contract 6 measures why: nixpkgs defines that template unconditionally inside `mkIf cfg.enable`, so the assertion would fail on a correct implementation. The absence assertions above are on the branches where nixpkgs genuinely renders nothing.
15. **Neither materializer script has an optional branch, and every owner and mode in the table is pinned.** Write both scripts to store files and assert `missing or empty, skipping` is absent from each, that each declared credential name appears in a validation command in its own unit's script, and that the write idiom is a rename — the needle is `mv -T`, because `install` alone truncates in place and would leave a window where a consumer reads a partial key. Paired positive control: the `missing or empty, skipping` needle must still be *present* in `machineConfigurations.allod-dev`'s `system.activationScripts.netrc.text`, which is what the existing `netrc-activation` check reads, so the two checks together prove the branch was removed from one path and kept on the other.

    **The owner/mode table is a contract and nothing asserted it.** Contract 4 fixes an owner, a group and a mode for `<root>` and for all five files, and this plan's own Risk Assessment names a wrongly-owned or world-readable secret as the failure that matters most — so leaving those values unchecked while checking `mv -T` inverts the priorities. Assert, in the generated scripts: the `<root>` creation carries `0711` and `root:root`, and each file's staging `install` carries the exact `-o`, `-g` and `-m` arguments its table row specifies. Build every needle with `lib.escapeShellArg`, for the reason `nix.md` records — `escapeShellArg` returns its result *unquoted* for an ordinary path, so a needle that hardcodes quotes tests nixpkgs' quoting optimisation instead of the generator. A unit defined with `script` puts its payload in a separate `unit-script-*` derivation, so read that, not the fragment.

    Paired sabotage, **on the shared unit-mutation fixture, not a new one**: change one file's write to a wrong mode and a second file's to a wrong owner — `forge-token` at `0644` and `git-credentials` at `root:root` are the two that matter, one widening access and one removing the user's. Two needles, both pinned, no new forced system.
16. **The rendered `sshd_config`.** Read `config.environment.etc."ssh/sshd_config".source` for a microvm fixture. Assert exactly one `HostKey` line and that it points under the credential root. Paired sabotage: a fixture whose `extraConfig` is forced empty must have zero `HostKey` lines. This is the *only* guard against the compiled-in-default fallback: pinned `sshd -G -T` exits 0 with no host key configured, so nixpkgs' own build-time config check cannot catch it.
17. **`sshd-keygen` is masked** for a microvm fixture — its rendered unit is a symlink to `/dev/null` — and is a real unit with a non-empty script for `allod-dev`. This is the anti-regeneration proof; the option read `hostKeys == []` is not, because an empty `hostKeys` also produces an unloadable unit rather than a masked one, and the two are indistinguishable from the option value.
18. **The rendered `nix.conf`: append, do not force.** Read the built `/etc/nix/nix.conf` source. Assert the **last** `netrc-file =` occurrence in the file is the runtime path under the credential root.

    Not `mkForce`, and not "exactly one line". `nix.extraOptions` is `types.lines`, `allod/vm` `modules/guest-base.nix` defines `nix.extraOptions = "netrc-file = /etc/nix/netrc"` in it, and `mkForce` on a `lines` option discards **every** other definition. The original draft argued that counting `netrc-file =` lines would catch a future `allod/vm` change adding an unrelated `extraOptions` setting that `mkForce` swallowed — it cannot, because anything `mkForce` discarded is simply absent from the artifact the check reads. The count is over the wrong file.

    So append with `lib.mkAfter` and assert last-wins, which is how Nix reads a repeated setting anyway. The preservation control is what makes this real: inject one unrelated `extraOptions` line into the fixture (`lib.mkAfter` on a second definition), and assert it survives into the rendered `nix.conf` alongside the runtime `netrc-file` line. Deleting the inherited definitions is then a failing test rather than an invisible one.

    **The injected line must be a setting the pinned Nix actually knows — `warn-dirty = false`, not a synthetic name.** `pkgs.formats.nixConf`'s generator runs `nix config show` in its `checkPhase` and promotes every warning to an error (`sed -e 's/^warning:/error:/' | (! grep '^error:')`, under a `checkAllErrors` that defaults true). An invented setting name is exactly what produces `warning: unknown setting`, so it would fail the build of `nix.conf` itself — the *positive* fixture, for a reason that looks like the mechanism under test and is not. Any real setting works as the control; what it must not be is `netrc-file` or a name Nix does not recognize.

    `nix.settings.netrc-file` is **not** the simpler route, and the reason is mechanical: `pkgs.formats.nixConf`'s generator emits every `settings` key first and interpolates `extraOptions` last (pinned nixpkgs `pkgs/pkgs-lib/formats.nix`, the `text` block of `nixConf.generate`). A `settings`-supplied runtime path would therefore be *overridden* by the inherited legacy `extraOptions` line, silently. No cross-repo change is needed and none is proposed.
19. **Both rendered Git config files, and the helper they name.** Contract 4 sets out the design; this test reads both artifacts in their own rendering, then runs the helper itself.
    - `/etc/gitconfig`: read `config.environment.etc."gitconfig".text`. Assert the inherited `helper = store` is still present (nothing was forced away), that the appended block opens with its own `[credential]` header, that a bare `helper =` reset appears after that header, and that the *last* helper line is the read-only helper's absolute store path, unquoted. Assert the header **by section, not by presence**: the nearest preceding `[…]` line before the reset must be `[credential]`. A presence check is satisfied by the exact failure contract 4 describes — a third definition landing between the two blocks and re-homing both appended lines into some other section.

      **The section predicate carries a canary, and it needs no NixOS system to carry it.** The earlier claim that this assertion could not be proved failing was wrong about what it would cost: reaching the failure through a *real* third `gitconfig` definition needs another forced dev system, but the thing under test is a text predicate, and a text predicate can be run on text. Factor "the nearest preceding `[…]` line before the reset is `[credential]`" out as a function, run it in the same derivation over synthetic input — a `[credential]` section, then an intervening `[core]` section, then the bare `helper =` reset — and require it to reject. A matcher weakened to an unanchored "`[credential]` appears somewhere above" then fails its own canary. Same shape as test 22's two synthetic canaries, same cost, and it removes the last "anchor against a future change, not a proved-failing check" from this plan.
    - The user's file: read the microvm fixture's `home-manager.users.<user>.programs.git.settings.credential.helper` and the rendered `git/config` text. Assert the rendered form is `helper = ""` followed by `helper = "<helper store path>"` — quoted, because `lib.generators.toGitINI`'s `mkValueString` quotes every string, unlike the raw `types.lines` above.
    - In both files: no helper line after the final reset other than the read-only helper, so no bare `store` remains effective. Assert on the *ordered* list of helper values, not on presence. Additionally assert that **neither file names `store --file=`**: contract 4's whole correction is that the shared credential file must not be reachable by a helper that can write it, and the string that would undo it is exactly the one the earlier draft specified.

      **A bare-`store` needle must compare whole helper values, never substrings**, now that the correct helper is a store path: `/nix/store/…` contains the substring `store`, so a `hasInfix "store"` check reports the *correct* configuration as broken and a `hasInfix "store --file="` check is the only substring form that is safe. Extract the ordered helper list and compare elements — the same rule `nix.md` records for generated shell needles, arriving here for a different reason.
    - **The helper's own behaviour, run rather than read.** This is the assertion contract 4's correction actually rests on, and it is a `runCommand` with `pkgs.git` — no NixOS system, no boot. Stage a fixture credential file containing one `https://user:pass@host` line, `chmod 0640` it, record its bytes and mode, then with `credential.helper` set to the generated helper and `HOME` pointed at a scratch directory:
      - `git credential fill` for that host must return the staged username and password. This is the only operation any consumer needs, and it is the one the design must not break.
      - `git credential approve` and `git credential reject` must each exit 0 and leave the file's **bytes and mode unchanged**.

      **The negative control is the stock helper, and it is what makes this a measurement instead of a citation.** Run the same three operations against the same staged file with `credential.helper = "store --file=<file>"`, and require the mode to change. That reproduces the blocker in the check: it proves `git credential approve` really does invoke the write path unprompted and really does reset permissions, on the pinned git, rather than taking `git-credential-store.adoc`'s word for it. If that control ever stops changing the mode, the read-only helper has become unnecessary and this plan's contract 4 is the thing to revisit.

      One honest limit, stated so a later pass does not read more into a green run than is there: a Nix build sandbox runs as a single user, so *ownership* cannot vary inside it and the ownership half of the guarantee is not measured here. What is measured is that the helper never opens the file for writing at all, which is the property ownership preservation follows from; the owner values themselves are pinned in the generated script by test 15.

    Paired sabotages: a fixture that drops the reset from either file must fail; a fixture that reverses the order within a file must fail; a fixture that fixes only `/etc/gitconfig` and leaves the user's `git/config` at bare `store` must fail, which is the failure this test was added for; and a fixture that names `store --file=<root>/git-credentials` in either file — the pre-correction design — must fail the no-writing-helper needle.
20. **The rendered `~/.ssh/config`, not the option that produced it.** This section opens by banning an option read in place of an artifact, and this test was written as one. The artifact is text: pinned Home Manager's `programs.ssh` writes `home-manager.users.<user>.home.file.".ssh/config".text`, rendering each identity as an indented, unquoted `  IdentityFile <path>` line inside its host block. Read that text for a microvm fixture and assert the Forge host's block carries exactly one `IdentityFile` line and that it names `<root>/forge-ssh-key`. Comparing `programs.ssh.matchBlocks.<forgeHost>.identityFile` first, as a cheap check before the text is built, is fine — it is not the assertion.

    Paired sabotage: make `allod/profiles`' `~/.ssh/<key>` win instead. The direct route is a higher-precedence definition on the profiles side (`lib.mkOverride 10`), because simply demoting archetypes' `mkForce` to plain priority *collides* with profiles' own plain definition rather than losing to it — both land at priority 100. **The needle must tolerate both outcomes**, since both prove the same thing: either the runtime path is absent from the rendered text with `~/.ssh/<key>` in its place, or evaluation fails with the module system's conflicting-definition error naming `identityFile`. Pinned Home Manager types that option `either (listOf str) (nullOr str)`, so two plain-priority string definitions conflict rather than concatenating into two `IdentityFile` lines.
21. **`FORGE_TOKEN_FILE`.** Assert the rendered `environment.sessionVariables` names `<root>/forge-token` for microvm and is unset for libvirt.
22. **Closure scan, with a control per detector.** Build the closure of a microvm dev fixture's `system.build.toplevel` and assert no path under it is or contains a `.age` file, an age/rage identity, or an OpenSSH private key.

    Three detectors need three controls, and the libvirt closure supplies only one. `machineConfigurations.allod-dev`'s closure must still contain the two `.age` ciphertexts — that proves traversal happens and that the filename predicate fires, and it proves nothing about the other two, which could both be deleted with this check still green. So also run the age-identity predicate over a store file containing the literal `AGE-SECRET-KEY-1` prefix and the private-key predicate over a store file containing an `-----BEGIN OPENSSH PRIVATE KEY-----` header, and require each to reject its own canary. Synthetic strings written by the check; no real key material, no generated key, and no second NixOS fixture.
23. **No consumer path under a persistent mount, scanned over contract 14's whole surface.** Parent contract 14 covers *"Generated activation text, Home Manager output, and unit definitions"* — all of it, including a mutation that adds a persistent copy. A scanner over a hand-written list of seven artifacts does not cover that: a copy in an unrelated activation script or an unrelated unit passes, and the earlier form of this test named exactly seven artifacts.

    **So enumerate, then filter, then scan.** Enumerate every generated artifact of the three kinds the parent names, on a microvm dev fixture:
    - every attribute of `config.system.activationScripts`, by its `text`;
    - every unit in `config.systemd.units`, by its fragment *and* by the `unit-script-*` derivation a `script`-defined unit puts its payload in;
    - every Home Manager artifact for the dev user: the activation script, `home.file.*` rendered texts, and the rendered `git/config` and `~/.ssh/config`.

    Select from that enumeration every artifact whose text mentions `<root>` or any declared credential name, and scan the selected set for any literal equal to or under a declared `microvm.volumes` mount point. The two-step shape is what keeps it affordable: the enumeration is cheap because it is a text scan of things the fixture renders anyway, and only credential-touching artifacts get the expensive comparison. It also self-corrects — a new activation script or unit that touches a credential is picked up without anyone remembering to add it to a list, which is the failure mode the seven-artifact version had built in.

    **Its paired sabotage rides the shared unit-mutation fixture and costs no new forced system.** The earlier form of this test correctly said "extend the fixture tests 13 to 15 share" while the memory budget separately counted it as a standalone thunk; the budget was wrong and the test was right. The scanner needs only a unique persistent-destination literal in an artifact something already renders, and that fixture already renders every artifact this test reads.

    **The mutation goes in an artifact the scanner has no other reason to look at**, which is the whole point of widening the surface: add a `system.activationScripts.<name>` to the shared fixture whose text copies a materialized credential under the dev home mount (`cp <root>/forge-token /home/<user>/.forge-token`; the volumes module declares `/home/<user>` as a `microvm.volumes` mountPoint), and require the scanner to name that literal. Putting it back in the Forge unit would only prove the scanner still reads the artifact it always read. The mutation is exactly the class the scanner exists for: a *copy* whose destination is durable, not a mis-set option.

    Test 9 does not cover any of this — that fixture sabotages the contract-14 *assertion*, so it fails at evaluation and the scanner never runs against the artifacts at all. `architecture.md` principle 11 and the parent's validator-validation rule are the same rule, and test 22 obeys it with two synthetic canaries.

### Existing checks that must be made explicitly libvirt-scoped

All three of these encode libvirt paths — `/root/.git-credentials`, `/etc/nix/netrc`, `/root/.netrc`, `/home/<u>/.netrc` — and all three reach them by *accident*: they read machines that happen to be libvirt today. Scope commits to making that explicit, and "explicit" means a runtime condition in the code, not a comment above it. The first microvm machine in a private inventory is the event that turns each of these from passing to confusingly failing, and it is an event no public check can produce.

24. **`netrc-activation` states the runtime its expectations describe, and asserts the complement.** It keeps reading `allod-dev` and keeps requiring the skip branch, and test 15 above turns that into half of a paired proof. Add the runtime condition: apply the netrc expectations only where `machines.<name>.runtime == "libvirt"`, and assert the complementary shape for microvm — that a microvm machine has **no** `system.activationScripts.netrc` at all. Two positive statements, one per runtime, rather than a skip: a check that silently declines to examine a machine is the silent fallback `architecture.md` principle 11 forbids, and it is also how this check would pass while saying nothing.
25. **`dev-forge-opt-out` pins its fixtures to libvirt explicitly.** Its literal `expectedWithAccess` projection describes the libvirt `allod-dev` and would be wrong for a microvm one, but its `withTokens` helper calls `mkDevVm` with `name`, `identity` and `definition` and **no `runtime`** — so the runtime comes from `machines.allod-dev.runtime`, and the check silently follows inventory wherever it goes. Pass `runtime = "libvirt"` in `withTokens`. That is the one-line change that makes the literal true by construction instead of by coincidence, and it moves nothing in the four-machine no-op table, because `withTokens` builds a check-internal fixture rather than one of the four `nixosConfigurations`.

    Test 2's microvm cases stay built from `runtime-module-selection`'s `devFixture` rather than by widening this check or sharing its helper across two `let` scopes, so each check keeps its own fixtures.
26. **`credential-profiles` filters all three of its libvirt-shaped projections by runtime.** Not "a comment and, if it costs one line, a filter" — the filters are the fix, and there are three:
    - `profileOwnedForgejoTokenTargets` currently filters on `builtins.hasAttr target.system machineConfigurations`, which is membership, not runtime. Add `machines.${target.system}.runtime == "libvirt"`.
    - `profileOwnedGithubTargets` flattens `secrets.lib.githubCredentialTargets` with no machine filter at all. Add the same runtime condition. This one also interacts with contradiction 5: a microvm machine with a registered GitHub target fails evaluation by design, so this check must not additionally assert an age-secret shape for it.
    - `localRefreshErrorsForRef` resolves a system out of `machineConfigurations` and then compares against `/root/.git-credentials`, `/etc/nix/netrc`, `/root/.netrc`, `/home/<u>/.netrc` and `netrc-file = /etc/nix/netrc`. Gate that comparison on the same runtime condition.

    All three read `machines.<name>.runtime`, which is the same fact the builders already read for `platform` and `runtime` — no new input, no new lookup path.

## Rollback Plan

Revert the PR. Every declaration is additive and gated on a runtime no public machine selects, so the revert restores the exact prior derivation for every libvirt machine and returns a microvm machine to composing the agenix path it composed before.

The edits that are not new declarations are still no-ops to revert. `modules/home-shared.nix` gains a defaulted `credentialHelper ? null` argument whose null branch is today's `credential.helper = "store"`; `mkDevVm` gains a defaulted `forgeKey ? machines.${name}.forge_key` and a defaulted `githubCredentialTargets ? secrets.lib.githubCredentialTargets.${name} or []`; and `modules/github-credentials.nix` takes that list as a `targets` argument instead of computing it, which is the same expression by construction. Every existing call site omits all three arguments, which is what the four unchanged derivation paths prove.

The read-only credential helper is a new derivation composed only on the microvm branch, so reverting removes it from the store and from nothing else. No libvirt machine ever names it, and no Git configuration outside the microvm branch changes: `/etc/gitconfig` returns to `allod/vm`'s bare `store` and the user's `git/config` to `"store"`, which is what they hold today.

The three check edits of tests 24 to 26 are the only changes that touch a path a libvirt machine cares about, and they touch it only inside checks. Explicit `runtime == "libvirt"` filters in `credential-profiles`, an explicit `runtime = "libvirt"` on `dev-forge-opt-out`'s `withTokens` fixture, and a runtime-conditioned expectation in `netrc-activation` all evaluate to today's behaviour on today's all-libvirt inventory. Reverting them restores the accidental scoping and breaks nothing; leaving them in place after a revert would also break nothing, which is worth knowing if the revert is partial.

No rollback step touches key material, a ciphertext, a registry, a host path, or a disk image. Nothing in this change creates, moves, rotates or deletes a credential; it changes only which paths a generated configuration names.

A partial state is not reachable: the change is one commit in one repo with no migration, no activation-time side effect on any machine that exists today, and no host counterpart. A machine that had already been enabled on the microvm runtime — which the Risk Assessment gate forbids — would return to failing evaluation on the missing agenix identity, which is loud.
